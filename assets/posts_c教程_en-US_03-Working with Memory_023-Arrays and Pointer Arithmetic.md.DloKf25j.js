import{_ as n,o as s,c as e,a5 as t}from"./chunks/framework.DJo0M80U.js";const u=JSON.parse('{"title":"23. Arrays and Pointer Arithmetic","description":"The Little Book of C — 23. Arrays and Pointer Arithmetic","frontmatter":{"title":"23. Arrays and Pointer Arithmetic","date":"2026-07-04","categories":["C 教程"],"tags":["C","Little Book of C","Working with Memory"],"description":"The Little Book of C — 23. Arrays and Pointer Arithmetic","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","section":23,"sidebarWeight":23,"lang":"en-US","alternateEn":"/posts/c教程/en-US/03-Working with Memory/023-Arrays and Pointer Arithmetic","alternateZh":"/posts/c教程/zh-CN/03-内存/023-Arrays and Pointer Arithmetic"},"headers":[],"relativePath":"posts/c教程/en-US/03-Working with Memory/023-Arrays and Pointer Arithmetic.md","filePath":"posts/c教程/en-US/03-Working with Memory/023-Arrays and Pointer Arithmetic.md","lastUpdated":1790163617000}'),i={name:"posts/c教程/en-US/03-Working with Memory/023-Arrays and Pointer Arithmetic.md"};function p(r,a,o,l,c,d){return s(),e("div",null,[...a[0]||(a[0]=[t(`<p>[中文版本](/posts/c教程/zh-CN/03-内存/023-Arrays and Pointer Arithmetic)</p><p>An array is a block of consecutive memory cells that hold elements of the same type. Arrays and pointers are deeply connected in C, in fact, an array’s name often behaves like a pointer to its first element. Understanding how arrays and pointer arithmetic work together is key to writing fast, memory-efficient programs.</p><h4 id="declaring-and-using-arrays" tabindex="-1">Declaring and Using Arrays <a class="header-anchor" href="#declaring-and-using-arrays" aria-label="Permalink to &quot;Declaring and Using Arrays&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int numbers[5] = {10, 20, 30, 40, 50};</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) {</span></span>
<span class="line"><span>        printf(&quot;numbers[%d] = %d\\n&quot;, i, numbers[i]);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>numbers[0] = 10</span></span>
<span class="line"><span>numbers[1] = 20</span></span>
<span class="line"><span>numbers[2] = 30</span></span>
<span class="line"><span>numbers[3] = 40</span></span>
<span class="line"><span>numbers[4] = 50</span></span></code></pre></div><p>Here:</p><ul><li><code>numbers</code> is an array of five integers.</li><li>Each element is stored next to each other in memory.</li><li>The compiler knows each<code>int</code> takes the same number of bytes, so it can find<code>numbers[i]</code> quickly using pointer arithmetic.</li></ul><h4 id="array-name-as-a-pointer" tabindex="-1">Array Name as a Pointer <a class="header-anchor" href="#array-name-as-a-pointer" aria-label="Permalink to &quot;Array Name as a Pointer&quot;">​</a></h4><p>When you use an array’s name (without an index), it acts as a pointer to its first element.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int numbers[3] = {1, 2, 3};</span></span>
<span class="line"><span>int *p = numbers;  // same as &amp;numbers[0]</span></span>
<span class="line"><span>printf(&quot;%d %d %d\\n&quot;, *p, *(p + 1), *(p + 2));</span></span></code></pre></div><p>Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1 2 3</span></span></code></pre></div><p>Each time you add 1 to the pointer, it moves forward by one element, not one byte, but one object of that type.</p><h4 id="tiny-code" tabindex="-1">Tiny Code <a class="header-anchor" href="#tiny-code" aria-label="Permalink to &quot;Tiny Code&quot;">​</a></h4><p>Here’s a complete example showing array access and pointer arithmetic:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int arr[5] = {2, 4, 6, 8, 10};</span></span>
<span class="line"><span>    int *ptr = arr; // arr decays to pointer to arr[0]</span></span>
<span class="line"><span>    printf(&quot;Accessing with array index:\\n&quot;);</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) {</span></span>
<span class="line"><span>        printf(&quot;arr[%d] = %d\\n&quot;, i, arr[i]);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    printf(&quot;\\nAccessing with pointer arithmetic:\\n&quot;);</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) {</span></span>
<span class="line"><span>        printf(&quot;*(ptr + %d) = %d\\n&quot;, i, *(ptr + i));</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    printf(&quot;\\nAddresses in memory:\\n&quot;);</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) {</span></span>
<span class="line"><span>        printf(&quot;&amp;arr[%d] = %p\\n&quot;, i, (void *)&amp;arr[i]);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Compile and run:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gcc array_pointer.c -o array_pointer</span></span>
<span class="line"><span>./array_pointer</span></span></code></pre></div><p>Output (addresses will differ):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Accessing with array index:</span></span>
<span class="line"><span>arr[0] = 2</span></span>
<span class="line"><span>arr[1] = 4</span></span>
<span class="line"><span>arr[2] = 6</span></span>
<span class="line"><span>arr[3] = 8</span></span>
<span class="line"><span>arr[4] = 10</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Accessing with pointer arithmetic:</span></span>
<span class="line"><span>*(ptr + 0) = 2</span></span>
<span class="line"><span>*(ptr + 1) = 4</span></span>
<span class="line"><span>*(ptr + 2) = 6</span></span>
<span class="line"><span>*(ptr + 3) = 8</span></span>
<span class="line"><span>*(ptr + 4) = 10</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Addresses in memory:</span></span>
<span class="line"><span>&amp;arr[0] = 0x7ffcc73f9a60</span></span>
<span class="line"><span>&amp;arr[1] = 0x7ffcc73f9a64</span></span>
<span class="line"><span>&amp;arr[2] = 0x7ffcc73f9a68</span></span>
<span class="line"><span>&amp;arr[3] = 0x7ffcc73f9a6c</span></span>
<span class="line"><span>&amp;arr[4] = 0x7ffcc73f9a70</span></span></code></pre></div><p>You can see that each element sits 4 bytes apart (typical size of<code>int</code>).</p><h4 id="pointer-arithmetic-rules" tabindex="-1">Pointer Arithmetic Rules <a class="header-anchor" href="#pointer-arithmetic-rules" aria-label="Permalink to &quot;Pointer Arithmetic Rules&quot;">​</a></h4><p>When you move pointers, C automatically scales by the size of the type they point to:</p><table tabindex="0"><thead><tr><th>Expression</th><th>Meaning</th></tr></thead><tbody><tr><td><code>p + 1</code></td><td>Move to the next element</td></tr><tr><td><code>p - 1</code></td><td>Move to the previous element</td></tr><tr><td><code>*(p + i)</code></td><td>Access the i-th element after the current one</td></tr><tr><td><code>p2 - p1</code></td><td>Returns the number of elements between two pointers</td></tr></tbody></table><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *start = arr;</span></span>
<span class="line"><span>int *end = arr + 5;</span></span>
<span class="line"><span>printf(&quot;Array length: %ld\\n&quot;, end - start); // prints 5</span></span></code></pre></div><h4 id="common-pitfalls" tabindex="-1">Common Pitfalls <a class="header-anchor" href="#common-pitfalls" aria-label="Permalink to &quot;Common Pitfalls&quot;">​</a></h4><p>Out-of-bounds access Accessing memory beyond an array’s valid range leads to undefined behavior:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>arr[5] = 99; // invalid! array has only indices 0-4</span></span></code></pre></div><p>Array decay Arrays “decay” to pointers when passed to functions, they lose size information. You must pass the length manually.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>void print_array(int *arr, int len);</span></span></code></pre></div><p>Pointer confusion Remember that<code>arr[i]</code> and<code>*(arr + i)</code> mean the same thing. Mixing them is fine, but be consistent for readability.</p><h4 id="why-it-matters" tabindex="-1">Why It Matters <a class="header-anchor" href="#why-it-matters" aria-label="Permalink to &quot;Why It Matters&quot;">​</a></h4><p>Arrays and pointers form the foundation of C data structures. You’ll use them to build:</p><ul><li>Strings (arrays of<code>char</code>)</li><li>Matrices (arrays of arrays)</li><li>Linked lists and trees (via pointer arithmetic)</li></ul><p>Once you’re comfortable thinking of arrays as contiguous memory blocks accessed through pointers, you can start designing your own data structures like a real systems programmer.</p><h4 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h4><ol><li>Write a function that prints an array using only pointers (no<code>[]</code> syntax).</li><li>Create an array of<code>char</code> and print it as a string and as separate characters.</li><li>Declare an array of 10 numbers, then use pointers to sum them.</li><li>Print the address difference between two elements.</li><li>Create a two-dimensional array and print it with nested loops.</li></ol><p>Arrays and pointers are two sides of the same coin in C. Once you understand their connection, you’ll see how powerful, and elegant, direct memory access can be.</p>`,40)])])}const m=n(i,[["render",p]]);export{u as __pageData,m as default};
