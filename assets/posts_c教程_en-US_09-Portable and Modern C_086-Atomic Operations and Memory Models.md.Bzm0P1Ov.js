import{_ as a,o as s,c as t,a5 as n}from"./chunks/framework.DJo0M80U.js";const m=JSON.parse('{"title":"86. Atomic Operations and Memory Models","description":"The Little Book of C — 86. Atomic Operations and Memory Models","frontmatter":{"title":"86. Atomic Operations and Memory Models","date":"2026-07-04","categories":["C 教程"],"tags":["C","Little Book of C","Portable and Modern C"],"description":"The Little Book of C — 86. Atomic Operations and Memory Models","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","section":86,"sidebarWeight":86,"lang":"en-US","alternateEn":"/posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models","alternateZh":"/posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models"},"headers":[],"relativePath":"posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models.md","filePath":"posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models.md","lastUpdated":1790163617000}'),o={name:"posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models.md"};function i(p,e,c,r,l,d){return s(),t("div",null,[...e[0]||(e[0]=[n(`<p>[中文版本](/posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models)</p><p>When multiple threads share data, you usually protect that data with locks like<code>pthread_mutex_t</code>. But sometimes, you need something faster, a way to perform an update that can’t be interrupted, even across threads. That’s where atomic operations come in.</p><p>This section introduces atomic operations in C and how the memory model ensures your program behaves predictably across cores.</p><h4 id="step-1-what-does-atomic-mean" tabindex="-1">Step 1. What Does “Atomic” Mean? <a class="header-anchor" href="#step-1-what-does-atomic-mean" aria-label="Permalink to &quot;Step 1. What Does “Atomic” Mean?&quot;">​</a></h4><p>An atomic operation is one that happens all at once, it can’t be divided or interrupted.</p><p>Example idea: If two threads both run<code>counter++</code> at the same time:</p><ul><li>Without atomicity → race condition.</li><li>With atomicity → one thread’s update completes fully before the other starts.</li></ul><p>Atomic operations are essential in lock-free algorithms, concurrent queues, and reference counters.</p><h4 id="step-2-the-problem-with-counter" tabindex="-1">Step 2. The Problem with counter++ <a class="header-anchor" href="#step-2-the-problem-with-counter" aria-label="Permalink to &quot;Step 2. The Problem with counter++&quot;">​</a></h4><p>This line looks simple:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>counter++;</span></span></code></pre></div><p>But under the hood, it’s three separate steps:</p><ol><li>Load<code>counter</code> from memory.</li><li>Increment it.</li><li>Store it back.</li></ol><p>Two threads doing this at once can lose updates:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread A: load(5)</span></span>
<span class="line"><span>Thread B: load(5)</span></span>
<span class="line"><span>Thread A: store(6)</span></span>
<span class="line"><span>Thread B: store(6)</span></span></code></pre></div><p>Result: one increment lost, final value should be 7 but ends up 6.</p><h4 id="step-3-using-atomic-types" tabindex="-1">Step 3. Using Atomic Types <a class="header-anchor" href="#step-3-using-atomic-types" aria-label="Permalink to &quot;Step 3. Using Atomic Types&quot;">​</a></h4><p>C11 introduced<code>&lt;stdatomic.h&gt;</code>, a portable way to use atomic operations.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    atomic_int counter = 0;</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, counter); // 2</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>No locks. No race conditions. The<code>atomic_*</code> functions guarantee the operations are atomic at the hardware level.</p><h4 id="step-4-common-atomic-functions" tabindex="-1">Step 4. Common Atomic Functions <a class="header-anchor" href="#step-4-common-atomic-functions" aria-label="Permalink to &quot;Step 4. Common Atomic Functions&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Function</th><th>Description</th></tr></thead><tbody><tr><td><code>atomic_load</code></td><td>Read atomically</td></tr><tr><td><code>atomic_store</code></td><td>Write atomically</td></tr><tr><td><code>atomic_fetch_add</code></td><td>Add and return old value</td></tr><tr><td><code>atomic_fetch_sub</code></td><td>Subtract and return old value</td></tr><tr><td><code>atomic_exchange</code></td><td>Replace and return old value</td></tr><tr><td><code>atomic_compare_exchange_strong</code></td><td>Compare-and-swap</td></tr></tbody></table><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_compare_exchange_strong(&amp;counter, &amp;expected, desired);</span></span></code></pre></div><p>If<code>counter == expected</code>, replace it with<code>desired</code>. Otherwise, update<code>expected</code> with the current value.</p><h4 id="step-5-tiny-code-atomic-counter-with-threads" tabindex="-1">Step 5. Tiny Code: Atomic Counter with Threads <a class="header-anchor" href="#step-5-tiny-code-atomic-counter-with-threads" aria-label="Permalink to &quot;Step 5. Tiny Code: Atomic Counter with Threads&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>#include &lt;pthread.h&gt;</span></span>
<span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>atomic_int counter = 0;</span></span>
<span class="line"><span>void* work(void* arg) {</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 100000; i++)</span></span>
<span class="line"><span>        atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    return NULL;</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    pthread_t t1, t2;</span></span>
<span class="line"><span>    pthread_create(&amp;t1, NULL, work, NULL);</span></span>
<span class="line"><span>    pthread_create(&amp;t2, NULL, work, NULL);</span></span>
<span class="line"><span>    pthread_join(t1, NULL);</span></span>
<span class="line"><span>    pthread_join(t2, NULL);</span></span>
<span class="line"><span>    printf(&quot;Counter = %d\\n&quot;, counter);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Output is always<code>200000</code>, with no mutexes and no data races.</p><h4 id="step-6-relaxed-vs-sequential-consistency" tabindex="-1">Step 6. Relaxed vs Sequential Consistency <a class="header-anchor" href="#step-6-relaxed-vs-sequential-consistency" aria-label="Permalink to &quot;Step 6. Relaxed vs Sequential Consistency&quot;">​</a></h4><p>Atomic operations can have different memory orders. By default, they’re sequentially consistent, the strongest and safest ordering.</p><table tabindex="0"><thead><tr><th>Memory Order</th><th>Meaning</th></tr></thead><tbody><tr><td><code>memory_order_seq_cst</code></td><td>Global consistent order (default)</td></tr><tr><td><code>memory_order_relaxed</code></td><td>Only atomicity guaranteed</td></tr><tr><td><code>memory_order_acquire</code></td><td>Prevent reordering before load</td></tr><tr><td><code>memory_order_release</code></td><td>Prevent reordering after store</td></tr><tr><td><code>memory_order_acq_rel</code></td><td>Acquire + Release combo</td></tr></tbody></table><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_fetch_add_explicit(&amp;counter, 1, memory_order_relaxed);</span></span></code></pre></div><p>This is faster but weaker, use only when you understand your memory model.</p><h4 id="step-7-memory-barriers-and-visibility" tabindex="-1">Step 7. Memory Barriers and Visibility <a class="header-anchor" href="#step-7-memory-barriers-and-visibility" aria-label="Permalink to &quot;Step 7. Memory Barriers and Visibility&quot;">​</a></h4><p>Modern CPUs reorder reads/writes for performance. Atomics, fences, and locks control when updates become visible to other threads.</p><p>Example: Thread A writes<code>ready = 1</code>. Thread B waits until it sees<code>ready == 1</code>. If the compiler reorders memory operations, Thread B might not see the change.</p><p>Use:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_thread_fence(memory_order_seq_cst);</span></span></code></pre></div><p>to prevent reordering across the fence.</p><h4 id="step-8-compare-and-swap-cas" tabindex="-1">Step 8. Compare-and-Swap (CAS) <a class="header-anchor" href="#step-8-compare-and-swap-cas" aria-label="Permalink to &quot;Step 8. Compare-and-Swap (CAS)&quot;">​</a></h4><p>CAS is the backbone of lock-free data structures.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int expected = 0;</span></span>
<span class="line"><span>int desired = 1;</span></span>
<span class="line"><span>if (atomic_compare_exchange_strong(&amp;counter, &amp;expected, desired)) {</span></span>
<span class="line"><span>    printf(&quot;Swapped!\\n&quot;);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>It atomically checks if<code>counter == expected</code> and updates it, all in one instruction. This is used to build things like spinlocks, queues, and reference counters.</p><h4 id="step-9-spinlocks-with-atomics" tabindex="-1">Step 9. Spinlocks with Atomics <a class="header-anchor" href="#step-9-spinlocks-with-atomics" aria-label="Permalink to &quot;Step 9. Spinlocks with Atomics&quot;">​</a></h4><p>A spinlock keeps checking until it can acquire the lock.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;unistd.h&gt;</span></span>
<span class="line"><span>atomic_flag lock = ATOMIC_FLAG_INIT;</span></span>
<span class="line"><span>void lock_spin(void) {</span></span>
<span class="line"><span>    while (atomic_flag_test_and_set(&amp;lock))</span></span>
<span class="line"><span>        ;</span><span> // busy wait</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>void unlock_spin(void) {</span></span>
<span class="line"><span>    atomic_flag_clear(&amp;lock);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>This is efficient when the lock is held for a very short time. For longer waits, use<code>pthread_mutex_t</code> instead.</p><h4 id="step-10-tiny-code-atomic-reference-counter" tabindex="-1">Step 10. Tiny Code: Atomic Reference Counter <a class="header-anchor" href="#step-10-tiny-code-atomic-reference-counter" aria-label="Permalink to &quot;Step 10. Tiny Code: Atomic Reference Counter&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>typedef struct {</span></span>
<span class="line"><span>    atomic_int refcount;</span></span>
<span class="line"><span>} Object;</span></span>
<span class="line"><span>void retain(Object* obj) {</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;obj-&gt;refcount, 1);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>void release(Object* obj) {</span></span>
<span class="line"><span>    if (atomic_fetch_sub(&amp;obj-&gt;refcount, 1) == 1)</span></span>
<span class="line"><span>        printf(&quot;Object freed\\n&quot;);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    Object obj = { .refcount = 1 };</span></span>
<span class="line"><span>    retain(&amp;obj);</span></span>
<span class="line"><span>    release(&amp;obj);</span></span>
<span class="line"><span>    release(&amp;obj);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Object freed</span></span></code></pre></div><p>This is how many real-world systems (e.g. file handles, shared memory) track usage.</p><h4 id="why-it-matters" tabindex="-1">Why It Matters <a class="header-anchor" href="#why-it-matters" aria-label="Permalink to &quot;Why It Matters&quot;">​</a></h4><p>Atomic operations are the building blocks of lock-free programming. They allow you to write high-performance concurrent code without blocking other threads. The C memory model gives you guarantees to reason about correctness even across multiple CPU cores.</p><h4 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h4><ol><li>Replace a mutex counter with an atomic counter.</li><li>Implement a spinlock using<code>atomic_flag</code>.</li><li>Use<code>atomic_compare_exchange_strong</code> to build a simple CAS loop.</li><li>Test the difference between<code>memory_order_relaxed</code> and<code>seq_cst</code>.</li><li>Build a reference-counted structure using atomics.</li></ol><p>Next, you’ll explore using C with other languages (FFI), how to make C libraries callable from Python, Rust, and Go.</p>`,58)])])}const u=a(o,[["render",i]]);export{m as __pageData,u as default};
