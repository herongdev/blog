import{_ as s,o as n,c as e,a5 as t}from"./chunks/framework.DJo0M80U.js";const u=JSON.parse('{"title":"86. 原子操作和内存模型","description":"The Little Book of C 中文版 — 86. 原子操作和内存模型","frontmatter":{"title":"86. 原子操作和内存模型","date":"2026-07-04","lang":"zh-CN","categories":["C 教程"],"tags":["C","Little Book of C","09-可移植与现代 C","中文"],"description":"The Little Book of C 中文版 — 86. 原子操作和内存模型","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","translator":"机器辅助翻译（Google Translate）","section":86,"sidebarWeight":86,"alternateZh":"/posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models","alternateEn":"/posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models"},"headers":[],"relativePath":"posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models.md","filePath":"posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models.md","lastUpdated":1790163617000}'),p={name:"posts/c教程/zh-CN/09-可移植与现代 C/086-Atomic Operations and Memory Models.md"};function o(i,a,l,c,d,r){return n(),e("div",null,[...a[0]||(a[0]=[t(`<p>[English version](/posts/c教程/en-US/09-Portable and Modern C/086-Atomic Operations and Memory Models)</p><p>当多个线程共享数据时，您通常使用锁来保护该数据，例如<code>pthread_mutex_t</code>。但有时，您需要更快的速度，一种即使跨线程也不能中断的执行更新的方法。这就是原子操作的用武之地。</p><p>本节介绍 C 语言中的原子操作以及内存模型如何确保程序在多个内核之间的行为可预测。</p><h4 id="步骤-1-原子-是什么意思" tabindex="-1">步骤 1.“原子”是什么意思？ <a class="header-anchor" href="#步骤-1-原子-是什么意思" aria-label="Permalink to &quot;步骤 1.“原子”是什么意思？&quot;">​</a></h4><p>原子操作是一次性发生的操作，不能被分割或中断。</p><p>示例想法：如果两个线程都运行<code>counter++</code>同时：</p><ul><li>没有原子性→竞争条件。</li><li>具有原子性 → 一个线程的更新在另一个线程开始之前完全完成。</li></ul><p>原子操作在无锁算法、并发队列和引用计数器中至关重要。</p><h4 id="步骤-2-counter-的问题" tabindex="-1">步骤 2. counter++ 的问题 <a class="header-anchor" href="#步骤-2-counter-的问题" aria-label="Permalink to &quot;步骤 2. counter++ 的问题&quot;">​</a></h4><p>这行看起来很简单：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>counter++;</span></span></code></pre></div><p>但在幕后，它是三个独立的步骤：</p><ol><li>负载<code>counter</code>从记忆中。</li><li>增加它。</li><li>将其存放回去。</li></ol><p>两个线程同时执行此操作可能会丢失更新：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Thread A: load(5)</span></span>
<span class="line"><span>Thread B: load(5)</span></span>
<span class="line"><span>Thread A: store(6)</span></span>
<span class="line"><span>Thread B: store(6)</span></span></code></pre></div><p>结果：丢失了一个增量，最终值应该是 7，但最终却是 6。</p><h4 id="步骤-3-使用原子类型" tabindex="-1">步骤 3. 使用原子类型 <a class="header-anchor" href="#步骤-3-使用原子类型" aria-label="Permalink to &quot;步骤 3. 使用原子类型&quot;">​</a></h4><p>C11介绍<code>&lt;stdatomic.h&gt;</code>，一种使用原子操作的可移植方式。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    atomic_int counter = 0;</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, counter); // 2</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>没有锁。没有比赛条件。这<code>atomic_*</code>函数保证操作在硬件级别是原子的。</p><h4 id="步骤-4-常见原子函数" tabindex="-1">步骤 4. 常见原子函数 <a class="header-anchor" href="#步骤-4-常见原子函数" aria-label="Permalink to &quot;步骤 4. 常见原子函数&quot;">​</a></h4><table tabindex="0"><thead><tr><th>功能</th><th>描述</th></tr></thead><tbody><tr><td><code>atomic_load</code></td><td>原子读取</td></tr><tr><td><code>atomic_store</code></td><td>原子写入</td></tr><tr><td><code>atomic_fetch_add</code></td><td>添加并返回旧值</td></tr><tr><td><code>atomic_fetch_sub</code></td><td>减去并返回旧值</td></tr><tr><td><code>atomic_exchange</code></td><td>替换并返回旧值</td></tr><tr><td><code>atomic_compare_exchange_strong</code></td><td>比较和交换</td></tr></tbody></table><p>例子：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_compare_exchange_strong(&amp;counter, &amp;expected, desired);</span></span></code></pre></div><p>如果<code>counter == expected</code>，将其替换为<code>desired</code>。否则，更新<code>expected</code>与当前值。</p><h4 id="步骤-5-小代码-带线程的原子计数器" tabindex="-1">步骤 5. 小代码：带线程的原子计数器 <a class="header-anchor" href="#步骤-5-小代码-带线程的原子计数器" aria-label="Permalink to &quot;步骤 5. 小代码：带线程的原子计数器&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>#include &lt;pthread.h&gt;</span></span>
<span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>atomic_int counter = 0;</span></span>
<span class="line"><span>void* work(void* arg) {</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 100000; i++)</span></span>
<span class="line"><span>        atomic_fetch_add(&amp;counter, 1);</span></span>
<span class="line"><span>    return NULL;</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    pthread_t t1, t2;</span></span>
<span class="line"><span>    pthread_create(&amp;t1, NULL, work, NULL);</span></span>
<span class="line"><span>    pthread_create(&amp;t2, NULL, work, NULL);</span></span>
<span class="line"><span>    pthread_join(t1, NULL);</span></span>
<span class="line"><span>    pthread_join(t2, NULL);</span></span>
<span class="line"><span>    printf(&quot;Counter = %d\\n&quot;, counter);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>输出始终是<code>200000</code>，没有互斥锁，也没有数据竞争。</p><h4 id="第-6-步-宽松一致性与顺序一致性" tabindex="-1">第 6 步：宽松一致性与顺序一致性 <a class="header-anchor" href="#第-6-步-宽松一致性与顺序一致性" aria-label="Permalink to &quot;第 6 步：宽松一致性与顺序一致性&quot;">​</a></h4><p>原子操作可以有不同的内存顺序。默认情况下，它们是顺序一致的、最强且最安全的排序。</p><table tabindex="0"><thead><tr><th>记忆顺序</th><th>意义</th></tr></thead><tbody><tr><td><code>memory_order_seq_cst</code></td><td>全局一致顺序（默认）</td></tr><tr><td><code>memory_order_relaxed</code></td><td>只保证原子性</td></tr><tr><td><code>memory_order_acquire</code></td><td>防止加载前重新排序</td></tr><tr><td><code>memory_order_release</code></td><td>防止商店后重新订购</td></tr><tr><td><code>memory_order_acq_rel</code></td><td>获取+释放组合</td></tr></tbody></table><p>例子：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_fetch_add_explicit(&amp;counter, 1, memory_order_relaxed);</span></span></code></pre></div><p>这更快但更弱，只有当你了解你的内存模型时才使用。</p><h4 id="步骤-7-内存屏障和可见性" tabindex="-1">步骤 7. 内存屏障和可见性 <a class="header-anchor" href="#步骤-7-内存屏障和可见性" aria-label="Permalink to &quot;步骤 7. 内存屏障和可见性&quot;">​</a></h4><p>现代 CPU 会重新排序读/写以提高性能。原子、栅栏和锁控制更新何时对其他线程可见。</p><p>示例：线程 A 写入<code>ready = 1</code>。线程B等待直到它看到<code>ready == 1</code>。如果编译器重新排序内存操作，线程 B 可能看不到更改。</p><p>使用：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>atomic_thread_fence(memory_order_seq_cst);</span></span></code></pre></div><p>以防止跨越栅栏重新排序。</p><h4 id="步骤-8-比较和交换-cas" tabindex="-1">步骤 8. 比较和交换 (CAS) <a class="header-anchor" href="#步骤-8-比较和交换-cas" aria-label="Permalink to &quot;步骤 8. 比较和交换 (CAS)&quot;">​</a></h4><p>CAS 是无锁数据结构的支柱。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int expected = 0;</span></span>
<span class="line"><span>int desired = 1;</span></span>
<span class="line"><span>if (atomic_compare_exchange_strong(&amp;counter, &amp;expected, desired)) {</span></span>
<span class="line"><span>    printf(&quot;Swapped!\\n&quot;);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>它自动检查是否<code>counter == expected</code>并更新它，全部通过一条指令完成。这用于构建诸如自旋锁、队列和引用计数器之类的东西。</p><h4 id="步骤-9-使用原子的自旋锁" tabindex="-1">步骤 9. 使用原子的自旋锁 <a class="header-anchor" href="#步骤-9-使用原子的自旋锁" aria-label="Permalink to &quot;步骤 9. 使用原子的自旋锁&quot;">​</a></h4><p>自旋锁不断检查，直到它可以获得锁。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;unistd.h&gt;</span></span>
<span class="line"><span>atomic_flag lock = ATOMIC_FLAG_INIT;</span></span>
<span class="line"><span>void lock_spin(void) {</span></span>
<span class="line"><span>    while (atomic_flag_test_and_set(&amp;lock))</span></span>
<span class="line"><span>        ;</span><span> // busy wait</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>void unlock_spin(void) {</span></span>
<span class="line"><span>    atomic_flag_clear(&amp;lock);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>当锁持有时间很短时，这是有效的。如果等待时间较长，请使用<code>pthread_mutex_t</code>反而。</p><h4 id="步骤-10-小代码-原子参考计数器" tabindex="-1">步骤 10. 小代码：原子参考计数器 <a class="header-anchor" href="#步骤-10-小代码-原子参考计数器" aria-label="Permalink to &quot;步骤 10. 小代码：原子参考计数器&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdatomic.h&gt;</span></span>
<span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>typedef struct {</span></span>
<span class="line"><span>    atomic_int refcount;</span></span>
<span class="line"><span>} Object;</span></span>
<span class="line"><span>void retain(Object* obj) {</span></span>
<span class="line"><span>    atomic_fetch_add(&amp;obj-&gt;refcount, 1);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>void release(Object* obj) {</span></span>
<span class="line"><span>    if (atomic_fetch_sub(&amp;obj-&gt;refcount, 1) == 1)</span></span>
<span class="line"><span>        printf(&quot;Object freed\\n&quot;);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    Object obj = { .refcount = 1 };</span></span>
<span class="line"><span>    retain(&amp;obj);</span></span>
<span class="line"><span>    release(&amp;obj);</span></span>
<span class="line"><span>    release(&amp;obj);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>输出：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Object freed</span></span></code></pre></div><p>这就是许多现实世界的系统（例如文件句柄、共享内存）跟踪使用情况的数量。</p><h4 id="为什么它很重要" tabindex="-1">为什么它很重要 <a class="header-anchor" href="#为什么它很重要" aria-label="Permalink to &quot;为什么它很重要&quot;">​</a></h4><p>原子操作是无锁编程的构建块。它们允许您编写高性能并发代码，而不会阻塞其他线程。 C 内存模型可以保证即使跨多个 CPU 内核也能推理出正确性。</p><h4 id="自己尝试一下" tabindex="-1">自己尝试一下 <a class="header-anchor" href="#自己尝试一下" aria-label="Permalink to &quot;自己尝试一下&quot;">​</a></h4><ol><li>用原子计数器替换互斥计数器。</li><li>使用以下方法实现自旋锁<code>atomic_flag</code>.</li><li>使用<code>atomic_compare_exchange_strong</code>构建一个简单的 CAS 循环。</li><li>测试之间的差异<code>memory_order_relaxed</code>和<code>seq_cst</code>.</li><li>使用原子构建引用计数结构。</li></ol><p>接下来，您将探索将 C 与其他语言 (FFI) 结合使用，以及如何使 C 库可从 Python、Rust 和 Go 调用。</p>`,58)])])}const m=s(p,[["render",o]]);export{u as __pageData,m as default};
