import{_ as r,o as s,c as o,a5 as n}from"./chunks/framework.DJo0M80U.js";const m=JSON.parse('{"title":"JavaScript 事件循环","description":"下面从 JavaScript 事件循环 （Event Loop）到 React 或浏览器渲染 的顺序，再到一些特殊 API（如 requestIdleCallback、requestAnimationFrame 等）进行 一体化 的讲解，帮助你理解它们的先后顺序及应用场景。 1.。","frontmatter":{"title":"JavaScript 事件循环","date":"2026-08-11T00:00:00.000Z","categories":["JavaScript 系统教程"],"tags":["JavaScript","前端","教程","OneNote","异步编程"],"description":"下面从 JavaScript 事件循环 （Event Loop）到 React 或浏览器渲染 的顺序，再到一些特殊 API（如 requestIdleCallback、requestAnimationFrame 等）进行 一体化 的讲解，帮助你理解它们的先后顺序及应用场景。 1.。","sidebarWeight":29,"lastUpdated":false,"feed":false,"source":"onenote","sourceNote":"OneNote/b-原生js/11-异步编程/JavaScript 事件循环.md"},"headers":[],"relativePath":"posts/JavaScript系统教程/05-异步编程/JavaScript 事件循环.md","filePath":"posts/JavaScript系统教程/05-异步编程/JavaScript 事件循环.md"}'),e={name:"posts/JavaScript系统教程/05-异步编程/JavaScript 事件循环.md"};function i(l,t,a,g,c,p){return s(),o("div",null,[...t[0]||(t[0]=[n('<div><h1 id="javascript-事件循环" tabindex="-1">JavaScript 事件循环 <a class="header-anchor" href="#javascript-事件循环" aria-label="Permalink to &quot;JavaScript 事件循环&quot;">​</a></h1><blockquote><p>本节目标：理解“JavaScript 事件循环”的核心思路，并能把它用于实际开发或面试表达。 下面从 <strong>JavaScript 事件循环</strong>（Event Loop）到 <strong>React 或浏览器渲染</strong> 的顺序，再到一些特殊 API（如 requestIdleCallback、requestAnimationFrame 等）进行<strong>一体化</strong>的讲解，帮助你理解它们的先后顺序及应用场景。</p></blockquote><p><strong>1. JavaScript 事件循环（Event Loop）基础</strong><strong>事件循环</strong>是 JavaScript 运行时（浏览器或 Node.js）用来调度执行各类任务（宏任务、微任务等）的机制。主要分为**宏任务（Macro Task）<strong>和</strong>微任务（Micro Task）**两大类别：</p><ol><li><strong>宏任务（Macro Task）</strong><ul><li>如：setTimeout, setInterval, setImmediate（Node 环境）, I/O 回调, UI 事件（click/keydown等） 等。</li><li>每个 <strong>宏任务</strong> 执行完后，才会进入下一轮循环。</li></ul></li><li><strong>微任务（Micro Task）</strong><ul><li>如：Promise.then/catch/finally 回调、 MutationObserver 回调、 queueMicrotask() 等。</li><li><strong>微任务优先于下一个宏任务</strong> 被执行；当前宏任务执行完后，会<strong>立即</strong>执行所有微任务队列的任务，然后才进入下一轮事件循环（或下一个宏任务）。</li></ul></li></ol><p><strong>任务调度顺序</strong>简化版（浏览器环境）：</p><ol><li>从宏任务队列里取一个宏任务，执行它的所有同步代码。</li><li>执行完后，查看<strong>微任务队列</strong>，依次执行其中所有微任务回调。</li><li>若有渲染（UI 更新）机会，会在这里进行<strong>浏览器渲染</strong>（浏览器可能依赖刷新率合并多次渲染）。</li><li>再取下一个宏任务，重复流程。</li></ol><p><strong>2. React 或浏览器的渲染时机</strong><strong>2.1 浏览器的渲染</strong> 浏览器通常在一次事件循环末尾或专门的<strong>渲染步骤</strong>里做 <strong>UI 绘制</strong>。</p><ul><li>在现代浏览器中，渲染<strong>节流</strong>到 ~16ms 一次（约 60FPS）或更低，具体取决于性能和空闲情况。</li><li>执行完所有微任务后，浏览器会检查是否需要渲染页面。如果有<strong>布局变更</strong>（layout）、<strong>样式变化</strong>，就进行一次渲染。</li></ul><p><strong>2.2 React 渲染</strong></p><ul><li><strong>React 18</strong> 以后引入了 <strong>并发模式</strong>（Concurrent Rendering），React 内部会把一些更新标记为<strong>可中断</strong>任务，用 <strong>调度器</strong>（基于 MessageChannel 或 scheduler API）来协调优先级。</li><li>当 React 需要更新 DOM 时，会将变更同步或异步地批量 flush 到浏览器 DOM，然后浏览器下次渲染周期会将其渲染到屏幕。</li><li>如果在一个 React 渲染过程中调用<strong>新的同步更新</strong>（flushSync），就可能出现嵌套 flush 的警告。</li></ul><p><strong>简言之</strong>：React 在某些情况下也会把更新放进微任务或<strong>调度队列</strong>，等当前事件循环结束后再统一批量渲染。并且浏览器最终会在事件循环末尾进行视图渲染（或者下一帧）。</p><p><strong>3. 一些特殊API</strong><strong>3.1 requestIdleCallback</strong><strong>requestIdleCallback(callback)</strong>：</p><ul><li>让浏览器在“空闲时间”调用你的回调函数。</li><li>当浏览器完成必要的渲染、布局等工作后，如果这一帧还有剩余空闲时间，才会执行 requestIdleCallback 回调。</li><li>常用于执行一些<strong>低优先级</strong>、<strong>不影响页面交互</strong>的任务（如预加载、数据上报或日志统计），在<strong>不会阻塞</strong>主线程关键操作时利用空闲时间处理。</li></ul><p>简单示例： requestIdleCallback((deadline) =&gt; { // deadline.timeRemaining() 可以查看当前空闲时间还剩多少ms while (deadline.timeRemaining() &gt; 0 &amp;&amp; tasks.length) { doTask(tasks.shift()); }}); <strong>浏览器</strong>会在下一帧或后续帧里，如果空闲时间大于 0ms，就执行该回调。若一直不空闲（CPU繁忙或帧率低），可能推迟很久才执行。 <strong>执行顺序</strong></p><ol><li>宏任务 &amp; 微任务完成后；</li><li>浏览器看下一帧有没有空闲时间段；</li><li>如果有，就执行 requestIdleCallback 回调（<strong>优先级低</strong>）。</li><li>比 setTimeout(fn, 0) 更晚，因为后者必然在下一轮宏任务立即执行，而 requestIdleCallback 只会在空闲时间执行。</li></ol><p><strong>兼容性</strong>：部分旧浏览器不支持，需要 polyfill。React 并非内置使用这个 API。</p><p><strong>3.2 requestAnimationFrame</strong><strong>requestAnimationFrame(callback)</strong>：</p><ul><li>让浏览器在<strong>下一帧绘制</strong>之前调用你的回调。</li><li>通常用于<strong>动画</strong>场景：可以在下一帧渲染前更新动画属性；浏览器能结合刷新率（~16.7ms/60Hz）来节流动画。</li><li>回调一般在浏览器渲染前执行，这样你更新 DOM/样式后能保证动画流畅。</li></ul><p>示例： function animate(timestamp) { // 更新 DOM 动画 requestAnimationFrame(animate)}requestAnimationFrame(animate) <strong>顺序</strong>：</p><ol><li>当前帧宏任务、微任务都执行完；</li><li>浏览器准备进行下一帧绘制前，调用你的 requestAnimationFrame 回调；</li><li>回调执行后再真正进行绘制。</li></ol><p>requestAnimationFrame 优先级比 setTimeout(fn, 0) 要高，因为它<strong>更靠近浏览器渲染</strong>阶段。</p><p><strong>3.3 queueMicrotask()</strong><strong>queueMicrotask()</strong>：</p><ul><li>将回调放到微任务队列，当当前宏任务结束后、下一个宏任务开始前立刻执行。</li><li>类似 Promise.resolve().then(), 但不需要 Promise 实例。</li></ul><p>详见之前的讲解：<strong>在同一轮事件循环内</strong>同步任务跑完后<strong>立即</strong>执行 microtask。</p><p><strong>4. 顺序整合</strong> 从<strong>最高优先级</strong>到<strong>最低优先级</strong>，可按以下简单思路理解：</p><ol><li><strong>当前宏任务</strong>（synchronous code）里所有同步操作；</li><li><strong>微任务</strong>（Promise.then, queueMicrotask）队列依次全部执行；</li><li>浏览器 <strong>可能</strong> 进行 <strong>一次渲染</strong>；（<strong>requestAnimationFrame</strong> 回调在渲染前触发）</li><li>执行 <strong>下一个宏任务</strong>（如 setTimeout(fn, 0)），事件循环继续；</li><li><strong>requestIdleCallback</strong> 回调只有在帧内剩余空闲时间里才执行，如果这帧没有空闲，就推迟到更后的时机。</li></ol><p>React 的并发更新可能在<strong>微任务阶段</strong>或<strong>一个调度机制</strong>里进行批量更新，然后在<strong>浏览器渲染前</strong>提交到 DOM。</p><p><strong>5. 总结</strong></p><ol><li><strong>JavaScript Event Loop</strong> 主要处理 <strong>宏任务</strong> → <strong>微任务</strong> → <strong>渲染</strong> → <strong>下一个宏任务</strong>。</li><li><strong>React</strong> 把状态更新放在内部调度器中，<strong>并发模式</strong>会打包/延迟这些更新；若有 flushSync 会立即同步。</li><li><strong>requestIdleCallback</strong>：在浏览器空闲时间段执行（优先级最低）。</li><li><strong>requestAnimationFrame</strong>：动画/绘制前触发，用于流畅动画。</li><li><strong>queueMicrotask()</strong>：插入微任务队列，立即在<strong>当前宏任务结束</strong>后调用（高于下一宏任务的优先级）。</li></ol><p><strong>在实际开发中</strong>：</p><ul><li><strong>对动画</strong>，常用 requestAnimationFrame；</li><li><strong>对低优先级任务</strong>，可用 requestIdleCallback；</li><li><strong>对微任务</strong>操作或避免同步阻塞的<strong>小逻辑</strong>，可用 queueMicrotask；</li><li><strong>对一般延迟</strong>可用 setTimeout(fn, 0)（下一轮宏任务），但优先级比微任务低。</li></ul><p>这样就能把握浏览器事件循环、渲染、React 更新以及各种调度API的<strong>先后时机</strong>和<strong>用法场景</strong>。</p></div>',1)])])}const d=r(e,[["render",i]]);export{m as __pageData,d as default};
