import{_ as s,o as n,c as p,a5 as e}from"./chunks/framework.DJo0M80U.js";const h=JSON.parse('{"title":"80. 练习：修复内存和逻辑错误","description":"The Little Book of C 中文版 — 80. 练习：修复内存和逻辑错误","frontmatter":{"title":"80. 练习：修复内存和逻辑错误","date":"2026-07-04","lang":"zh-CN","categories":["C 教程"],"tags":["C","Little Book of C","08-调试测试与 profiling","中文"],"description":"The Little Book of C 中文版 — 80. 练习：修复内存和逻辑错误","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","translator":"机器辅助翻译（Google Translate）","section":"80","sidebarWeight":"80","alternateZh":"/posts/c教程/zh-CN/08-调试测试与 profiling/080-Practice Fix Memory and Logic Bugs","alternateEn":"/posts/c教程/en-US/08-Debugging Testing Profiling/080-Practice Fix Memory and Logic Bugs"},"headers":[],"relativePath":"posts/c教程/zh-CN/08-调试测试与 profiling/080-Practice Fix Memory and Logic Bugs.md","filePath":"posts/c教程/zh-CN/08-调试测试与 profiling/080-Practice Fix Memory and Logic Bugs.md","lastUpdated":1790163617000}'),i={name:"posts/c教程/zh-CN/08-调试测试与 profiling/080-Practice Fix Memory and Logic Bugs.md"};function t(l,a,o,c,d,u){return n(),p("div",null,[...a[0]||(a[0]=[e(`<p>[English version](/posts/c教程/en-US/08-Debugging Testing Profiling/080-Practice Fix Memory and Logic Bugs)</p><h4 id="跟练交付物" tabindex="-1">跟练交付物 <a class="header-anchor" href="#跟练交付物" aria-label="Permalink to &quot;跟练交付物&quot;">​</a></h4><ul><li>已具备状态：完成第 071-079 课，能重新编译上一章示例。</li><li>工作目录：<code>~/c-course-labs/080-debug-lab</code>。</li><li>第一条命令：macOS / Linux 运行 <code>mkdir -p ~/c-course-labs/080-debug-lab &amp;&amp; cd ~/c-course-labs/080-debug-lab</code>；Windows PowerShell 运行 <code>New-Item -ItemType Directory -Force &quot;$HOME\\c-course-labs\\080-debug-lab&quot;; Set-Location &quot;$HOME\\c-course-labs\\080-debug-lab&quot;</code>。</li><li>成功证据：保留源码、可执行文件、<code>evidence.md</code>，并记录修复前失败、定位方法、修复后输出、内存检查或断言结果。</li><li>本章边界：本章目标是修复证据，不是追求功能数量；暂不要求完整测试框架或 CI。</li><li>重置方式：删除本章生成的可执行文件、临时数据和输出文件，保留源码与 <code>evidence.md</code> 方便复盘。</li></ul><p>现在是时候将您所学到的一切（调试、测试、断言、日志记录和分析）应用到实际损坏的代码中了。本节将引导您了解新程序员（甚至有经验的程序员）会遇到的一些常见的小 C 错误，并展示如何查找、理解和修复它们。</p><h4 id="步骤-1-bug-1-来自坏指针的分段错误" tabindex="-1">步骤 1. Bug #1，来自坏指针的分段错误 <a class="header-anchor" href="#步骤-1-bug-1-来自坏指针的分段错误" aria-label="Permalink to &quot;步骤 1. Bug #1，来自坏指针的分段错误&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int *p;</span></span>
<span class="line"><span>    *p = 10;</span><span>  // writing to uninitialized pointer</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, *p);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>症状：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Segmentation fault (core dumped)</span></span></code></pre></div><p>诊断：</p><ul><li>指针<code>p</code>从未被初始化。</li><li>它指向一个未定义的地址。</li></ul><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int x = 10;</span></span>
<span class="line"><span>    int *p = &amp;x;</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, *p);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>经验教训：在使用指针之前始终初始化指针。如果是动态的，则分配<code>malloc()</code>并检查<code>NULL</code>.</p><h4 id="步骤-2-bug-2-内存泄漏" tabindex="-1">步骤 2. Bug #2，内存泄漏 <a class="header-anchor" href="#步骤-2-bug-2-内存泄漏" aria-label="Permalink to &quot;步骤 2. Bug #2，内存泄漏&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdlib.h&gt;</span></span>
<span class="line"><span>void leak(void) {</span></span>
<span class="line"><span>    int *arr = malloc(10 * sizeof(int));</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 10; i++) arr[i] = i;</span></span>
<span class="line"><span>    // forgot to free</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 10000; i++) leak();</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>诊断：每次致电<code>leak()</code>分配内存并且从不释放它。使用 Valgrind 确认：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>valgrind ./a.out</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>void leak(void) {</span></span>
<span class="line"><span>    int *arr = malloc(10 * sizeof(int));</span></span>
<span class="line"><span>    if (!arr) return;</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 10; i++) arr[i] = i;</span></span>
<span class="line"><span>    free(arr);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>课程：每<code>malloc()</code>需要一个匹配的<code>free()</code>，无一例外。</p><h4 id="步骤-3-bug-3-相差一错误" tabindex="-1">步骤 3. Bug #3，相差一错误 <a class="header-anchor" href="#步骤-3-bug-3-相差一错误" aria-label="Permalink to &quot;步骤 3. Bug #3，相差一错误&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int nums[5] = {0, 1, 2, 3, 4};</span></span>
<span class="line"><span>    for (int i = 0; i &lt;= 5; i++)  // ❌ should be &lt; 5</span></span>
<span class="line"><span>        printf(&quot;%d &quot;, nums[i]);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>症状：有时会打印垃圾或段错误。</p><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for (int i = 0; i &lt; 5; i++)</span></span></code></pre></div><p>教训：差一错误是循环中最常见的错误。务必仔细检查边界条件。</p><h4 id="步骤-4-bug-4-释放后使用" tabindex="-1">步骤 4. Bug #4，释放后使用 <a class="header-anchor" href="#步骤-4-bug-4-释放后使用" aria-label="Permalink to &quot;步骤 4. Bug #4，释放后使用&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>#include &lt;stdlib.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int *x = malloc(sizeof(int));</span></span>
<span class="line"><span>    *x = 5;</span></span>
<span class="line"><span>    free(x);</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, *x);  // ❌ accessing freed memory</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>free(x);</span></span>
<span class="line"><span>x = NULL;</span></span></code></pre></div><p>现在：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if (x) printf(&quot;%d\\n&quot;, *x);</span></span></code></pre></div><p>教训：一旦释放了内存，它就不再是你的了，永远不要再碰它。</p><h4 id="步骤-5-bug-5-堆栈变量转义作用域" tabindex="-1">步骤 5. Bug #5，堆栈变量转义作用域 <a class="header-anchor" href="#步骤-5-bug-5-堆栈变量转义作用域" aria-label="Permalink to &quot;步骤 5. Bug #5，堆栈变量转义作用域&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *make_ptr(void) {</span></span>
<span class="line"><span>    int x = 10;</span></span>
<span class="line"><span>    return &amp;x;  // ❌ pointer to local variable</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int *p = make_ptr();</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, *p);  // UB</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *make_ptr(void) {</span></span>
<span class="line"><span>    int *x = malloc(sizeof(int));</span></span>
<span class="line"><span>    *x = 10;</span></span>
<span class="line"><span>    return x;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>并记住<code>free(p)</code>之后。</p><p>教训：永远不要返回局部变量的地址，它的生命周期在函数返回时结束。</p><h4 id="步骤-6-bug-6-缺少返回语句" tabindex="-1">步骤 6. Bug #6，缺少返回语句 <a class="header-anchor" href="#步骤-6-bug-6-缺少返回语句" aria-label="Permalink to &quot;步骤 6. Bug #6，缺少返回语句&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int add(int a, int b) {</span></span>
<span class="line"><span>    int c = a + b;</span></span>
<span class="line"><span>    // forgot to return</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    printf(&quot;%d\\n&quot;, add(2, 3));</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>return c;</span></span></code></pre></div><p>教训：如果函数的返回类型是非<code>void</code>，始终返回一个值。编译用<code>-Wall -Wextra</code>自动捕捉这个。</p><h4 id="步骤-7-bug-7-未初始化的变量" tabindex="-1">步骤 7.Bug #7，未初始化的变量 <a class="header-anchor" href="#步骤-7-bug-7-未初始化的变量" aria-label="Permalink to &quot;步骤 7.Bug #7，未初始化的变量&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int sum(void) {</span></span>
<span class="line"><span>    int s;</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 3; i++) s += i;  // s not initialized</span></span>
<span class="line"><span>    return s;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int s = 0;</span></span></code></pre></div><p>教训：在使用所有变量之前初始化它们，尤其是累加器。</p><h4 id="步骤-8-bug-8-混合签名和未签名" tabindex="-1">步骤 8. Bug #8，混合签名和未签名 <a class="header-anchor" href="#步骤-8-bug-8-混合签名和未签名" aria-label="Permalink to &quot;步骤 8. Bug #8，混合签名和未签名&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int a = -1;</span></span>
<span class="line"><span>    unsigned int b = 1;</span></span>
<span class="line"><span>    if (a &lt; b) printf(&quot;less\\n&quot;); else printf(&quot;greater\\n&quot;);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>输出：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>greater</span></span></code></pre></div><p>解释：<code>a</code>转换为无符号，因此它变成一个很大的正数。</p><p>修复：避免混合有符号和无符号类型。使用显式强制转换或一致类型。</p><h4 id="步骤-9-bug-9-缓冲区溢出" tabindex="-1">步骤 9. Bug #9，缓冲区溢出 <a class="header-anchor" href="#步骤-9-bug-9-缓冲区溢出" aria-label="Permalink to &quot;步骤 9. Bug #9，缓冲区溢出&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>#include &lt;string.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    char name[8];</span></span>
<span class="line"><span>    strcpy(name, &quot;Superlongname&quot;);  // ❌ too big</span></span>
<span class="line"><span>    printf(&quot;%s\\n&quot;, name);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>strncpy(name, &quot;Superlongname&quot;, sizeof(name) - 1);</span></span>
<span class="line"><span>name[7] = &#39;\\0&#39;;</span></span></code></pre></div><p>教训：永远不要相信输入大小，始终使用有界函数。</p><h4 id="步骤-10-bug-10-浮点比较" tabindex="-1">步骤 10. Bug #10，浮点比较 <a class="header-anchor" href="#步骤-10-bug-10-浮点比较" aria-label="Permalink to &quot;步骤 10. Bug #10，浮点比较&quot;">​</a></h4><p>越野车代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    float a = 0.1f * 3;</span></span>
<span class="line"><span>    if (a == 0.3f) printf(&quot;Equal\\n&quot;);</span></span>
<span class="line"><span>    else printf(&quot;Not equal\\n&quot;);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>输出：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Not equal</span></span></code></pre></div><p>使固定：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if (fabsf(a - 0.3f) &lt; 1e-6) printf(&quot;Equal\\n&quot;);</span></span></code></pre></div><p>教训：浮点数学是近似的，总是与公差进行比较。</p><h4 id="把它们放在一起" tabindex="-1">把它们放在一起 <a class="header-anchor" href="#把它们放在一起" aria-label="Permalink to &quot;把它们放在一起&quot;">​</a></h4><p>您可以结合所有这些技术：</p><ul><li>使用断言来捕获不可能的状态。</li><li>使用日志记录来跟踪事件。</li><li>运行 Valgrind 或 ASan 来检测内存错误。</li><li>使用单元测试来验证正确性。</li><li>如果仍然崩溃，请分析核心转储。</li></ul><p>每个调试工具都是一个镜头。一起使用它们可以看得更清楚。</p><h4 id="为什么它很重要" tabindex="-1">为什么它很重要 <a class="header-anchor" href="#为什么它很重要" aria-label="Permalink to &quot;为什么它很重要&quot;">​</a></h4><p>调试告诉您程序如何失败。每个修复的错误都会让您成为更加自信的系统工程师。 C 不会原谅错误，但它奖励精确性。</p><h4 id="自己尝试一下" tabindex="-1">自己尝试一下 <a class="header-anchor" href="#自己尝试一下" aria-label="Permalink to &quot;自己尝试一下&quot;">​</a></h4><ol><li>编写一个包含至少 3 个此类错误的程序。</li><li>在AddressSanitizer下运行它（<code>-fsanitize=address</code>). 3.一一修复每个bug。</li><li>记录导致该问题的原因以及解决该问题的方法。</li><li>将此作为个人调试套路，练习直至没有 bug 存活时间超过 10 分钟。</li></ol><p>接下来，我们将开始第 9 章：可移植和现代 C，您将学习如何编写可以在从嵌入式芯片到现代服务器的任何地方运行的 C。</p>`,86)])])}const g=s(i,[["render",t]]);export{h as __pageData,g as default};
