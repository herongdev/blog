import{_ as a,o as n,c as s,a5 as t}from"./chunks/framework.DJo0M80U.js";const u=JSON.parse('{"title":"26. Memory Leaks and Undefined Behavior","description":"The Little Book of C — 26. Memory Leaks and Undefined Behavior","frontmatter":{"title":"26. Memory Leaks and Undefined Behavior","date":"2026-07-04","categories":["C 教程"],"tags":["C","Little Book of C","Working with Memory"],"description":"The Little Book of C — 26. Memory Leaks and Undefined Behavior","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","section":26,"sidebarWeight":26,"lang":"en-US","alternateEn":"/posts/c教程/en-US/03-Working with Memory/026-Memory Leaks and Undefined Behavior","alternateZh":"/posts/c教程/zh-CN/03-内存/026-Memory Leaks and Undefined Behavior"},"headers":[],"relativePath":"posts/c教程/en-US/03-Working with Memory/026-Memory Leaks and Undefined Behavior.md","filePath":"posts/c教程/en-US/03-Working with Memory/026-Memory Leaks and Undefined Behavior.md","lastUpdated":1790163617000}'),i={name:"posts/c教程/en-US/03-Working with Memory/026-Memory Leaks and Undefined Behavior.md"};function o(r,e,l,p,d,c){return n(),s("div",null,[...e[0]||(e[0]=[t(`<p>[中文版本](/posts/c教程/zh-CN/03-内存/026-Memory Leaks and Undefined Behavior)</p><p>C gives you total control over memory, which means you can do anything you want, including things that should never be done. Two of the biggest dangers are memory leaks (when memory is never released) and undefined behavior (when the program does something unpredictable). Learning to avoid these is the key to writing stable, safe, and correct C programs.</p><h4 id="what-is-a-memory-leak" tabindex="-1">What Is a Memory Leak? <a class="header-anchor" href="#what-is-a-memory-leak" aria-label="Permalink to &quot;What Is a Memory Leak?&quot;">​</a></h4><p>A memory leak happens when you allocate memory on the heap and never free it. The memory stays reserved even though you can’t access it anymore.</p><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdlib.h&gt;</span></span>
<span class="line"><span>void leak(void) {</span></span>
<span class="line"><span>    int *data = malloc(100 * sizeof(int)); // allocated</span></span>
<span class="line"><span>    data[0] = 42;</span></span>
<span class="line"><span>    // forgot to free(data); memory is now lost</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>If<code>leak()</code> runs many times, your program consumes more and more memory until it crashes or slows down. In long-running programs (like servers), this is deadly.</p><p>Rule: Every<code>malloc</code>,<code>calloc</code>, or<code>realloc</code> must eventually be paired with a matching<code>free()</code>.</p><h4 id="tiny-code" tabindex="-1">Tiny Code <a class="header-anchor" href="#tiny-code" aria-label="Permalink to &quot;Tiny Code&quot;">​</a></h4><p>Let’s see leaks and fixes in action.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>#include &lt;stdlib.h&gt;</span></span>
<span class="line"><span>void with_leak(void) {</span></span>
<span class="line"><span>    int *arr = malloc(5 * sizeof(int));</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) arr[i] = i;</span></span>
<span class="line"><span>    printf(&quot;with_leak: allocated 5 ints, but not freed.\\n&quot;);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>void without_leak(void) {</span></span>
<span class="line"><span>    int *arr = malloc(5 * sizeof(int));</span></span>
<span class="line"><span>    for (int i = 0; i &lt; 5; i++) arr[i] = i;</span></span>
<span class="line"><span>    printf(&quot;without_leak: freeing memory.\\n&quot;);</span></span>
<span class="line"><span>    free(arr);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    with_leak();</span></span>
<span class="line"><span>    without_leak();</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Compile and run with Valgrind (a memory checker):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gcc leaks_demo.c -o leaks_demo</span></span>
<span class="line"><span>valgrind ./leaks_demo</span></span></code></pre></div><p>Valgrind output (simplified):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>==1234== HEAP SUMMARY:</span></span>
<span class="line"><span>==1234==    definitely lost: 20 bytes in 1 blocks</span></span>
<span class="line"><span>==1234==    indirectly lost: 0 bytes in 0 blocks</span></span>
<span class="line"><span>==1234== LEAK SUMMARY:</span></span>
<span class="line"><span>==1234==    1 blocks definitely lost</span></span></code></pre></div><p>You can see the first function leaked memory, while the second freed it properly.</p><h4 id="dangling-pointers" tabindex="-1">Dangling Pointers <a class="header-anchor" href="#dangling-pointers" aria-label="Permalink to &quot;Dangling Pointers&quot;">​</a></h4><p>A dangling pointer points to memory that has been freed or is otherwise invalid.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *p = malloc(sizeof(int));</span></span>
<span class="line"><span>*p = 10;</span></span>
<span class="line"><span>free(p);</span></span>
<span class="line"><span>printf(&quot;%d\\n&quot;, *p); // ❌ undefined behavior</span></span></code></pre></div><p>After<code>free(p)</code>, the pointer<code>p</code> still holds the old address, but that memory no longer belongs to you. Accessing it may crash, or appear to work, or corrupt data, you can’t rely on it.</p><p>Always nullify freed pointers:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>free(p);</span></span>
<span class="line"><span>p = NULL;</span></span></code></pre></div><h4 id="double-free" tabindex="-1">Double Free <a class="header-anchor" href="#double-free" aria-label="Permalink to &quot;Double Free&quot;">​</a></h4><p>Freeing the same memory twice also leads to undefined behavior:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *p = malloc(sizeof(int));</span></span>
<span class="line"><span>free(p);</span></span>
<span class="line"><span>free(p); // ❌ double free error</span></span></code></pre></div><p>Most modern OSes detect this and abort, but it’s still a critical bug.</p><h4 id="use-after-free" tabindex="-1">Use-After-Free <a class="header-anchor" href="#use-after-free" aria-label="Permalink to &quot;Use-After-Free&quot;">​</a></h4><p>This is one of the worst kinds of memory errors. It happens when you access memory after it’s been freed.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *arr = malloc(3 * sizeof(int));</span></span>
<span class="line"><span>arr[0] = 5;</span></span>
<span class="line"><span>free(arr);</span></span>
<span class="line"><span>arr[0] = 7; // ❌ use-after-free</span></span></code></pre></div><p>The compiler won’t catch this, but Valgrind will warn you.</p><h4 id="uninitialized-memory" tabindex="-1">Uninitialized Memory <a class="header-anchor" href="#uninitialized-memory" aria-label="Permalink to &quot;Uninitialized Memory&quot;">​</a></h4><p>Reading memory you never wrote to is also undefined:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int *arr = malloc(5 * sizeof(int));</span></span>
<span class="line"><span>printf(&quot;%d\\n&quot;, arr[2]); // ❌ uninitialized read</span></span></code></pre></div><p><code>malloc()</code> does not zero out memory, use<code>calloc()</code> if you need cleared data.</p><h4 id="common-causes-of-undefined-behavior" tabindex="-1">Common Causes of Undefined Behavior <a class="header-anchor" href="#common-causes-of-undefined-behavior" aria-label="Permalink to &quot;Common Causes of Undefined Behavior&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Example</th><th>Consequence</th></tr></thead><tbody><tr><td>Out-of-bounds access</td><td><code>arr[10]</code> in a 5-element array</td><td>Corrupts memory</td></tr><tr><td>Use-after-free</td><td>Dereferencing freed pointer</td><td>Crash or silent corruption</td></tr><tr><td>Null pointer dereference</td><td><code>*NULL</code></td><td>Crash</td></tr><tr><td>Division by zero</td><td><code>x / 0</code></td><td>Crash</td></tr><tr><td>Invalid pointer arithmetic</td><td><code>(int *)0 + 1</code></td><td>Undefined</td></tr><tr><td>Modifying string literal</td><td><code>char *s = &quot;hi&quot;; s[0]=&#39;H&#39;;</code></td><td>Crash</td></tr></tbody></table><h4 id="why-it-matters" tabindex="-1">Why It Matters <a class="header-anchor" href="#why-it-matters" aria-label="Permalink to &quot;Why It Matters&quot;">​</a></h4><p>Undefined behavior is not “just a bug.” It means anything can happen:</p><ul><li>Your program may seem fine but fail later.</li><li>Compiler optimizations may remove or reorder code unexpectedly.</li><li>The same code might work on one system and crash on another.</li></ul><p>In C, correctness is your responsibility. You must know when memory is valid, who owns it, and when to free it.</p><h4 id="defensive-techniques" tabindex="-1">Defensive Techniques <a class="header-anchor" href="#defensive-techniques" aria-label="Permalink to &quot;Defensive Techniques&quot;">​</a></h4><ol><li>Always check<code>malloc()</code> return values.</li><li>Initialize pointers to<code>NULL</code>.</li><li>Set pointers to<code>NULL</code> after freeing.</li><li>Use Valgrind (Linux) or AddressSanitizer (Clang/GCC) to detect leaks and invalid access.</li><li>Prefer small, testable functions, easier to verify memory ownership.</li><li>Avoid mixing stack and heap memory unless you’re certain of lifetimes.</li></ol><h4 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h4><ol><li>Write a small program that intentionally leaks memory. Run it under Valgrind.</li><li>Fix the leak by calling<code>free()</code> properly.</li><li>Create a dangling pointer and observe what happens (on some systems it crashes, on others not).</li><li>Experiment with<code>calloc()</code> to see how zero-initialized memory behaves.</li><li>Write a function that allocates memory and returns it, then ensure the caller frees it.</li></ol><h4 id="final-thought" tabindex="-1">Final Thought <a class="header-anchor" href="#final-thought" aria-label="Permalink to &quot;Final Thought&quot;">​</a></h4><p>Memory errors are the hardest bugs to track because they may not appear right away. But once you understand ownership, who allocates and who frees, memory in C becomes predictable, even elegant. This discipline is what separates casual C users from real systems programmers.</p>`,46)])])}const m=a(i,[["render",o]]);export{u as __pageData,m as default};
