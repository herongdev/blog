import{_ as e,o as r,c as t,a5 as o}from"./chunks/framework.DJo0M80U.js";const w=JSON.parse('{"title":"用 () = ({ ...props })","description":"你的问题是关于为什么在 watch 中使用 () \\\\ (\\\\{ ...props \\\\}) 而不是直接使用 () \\\\ props，以及为什么要解构 props。这是一个很棒的问题，涉及到 Vue 的响应式机制和 watch 的依赖追踪原理。我会详细解释原因，并分析两者的区别。 背景：。","frontmatter":{"title":"用 () = ({ ...props })","date":"2026-08-11T00:00:00.000Z","categories":["Vue 系统教程"],"tags":["Vue","Vue3","前端","教程","OneNote","模板、组件与交互"],"description":"你的问题是关于为什么在 watch 中使用 () \\\\ (\\\\{ ...props \\\\}) 而不是直接使用 () \\\\ props，以及为什么要解构 props。这是一个很棒的问题，涉及到 Vue 的响应式机制和 watch 的依赖追踪原理。我会详细解释原因，并分析两者的区别。 背景：。","sidebarWeight":49,"lastUpdated":false,"feed":false,"source":"onenote","sourceNote":"OneNote/f-vue/api/用 () = ({ ...props }).md"},"headers":[],"relativePath":"posts/Vue系统教程/02-模板、组件与交互/用 () = ({ ...props }).md","filePath":"posts/Vue系统教程/02-模板、组件与交互/用 () = ({ ...props }).md"}'),s={name:"posts/Vue系统教程/02-模板、组件与交互/用 () = ({ ...props }).md"};function l(n,p,i,a,g,c){return r(),t("div",null,[...p[0]||(p[0]=[o('<div><h1 id="用-props" tabindex="-1">用 () = ({ ...props }) <a class="header-anchor" href="#用-props" aria-label="Permalink to &quot;用 () = ({ ...props })&quot;">​</a></h1><blockquote><p>本节目标：理解“用 () = ({ ...props })”的核心思路，并能把它用于实际开发或面试表达。 你的问题是关于为什么在 watch 中使用 () =&gt; ({ ...props }) 而不是直接使用 () =&gt; props，以及为什么要解构 props。这是一个很棒的问题，涉及到 Vue 的响应式机制和 watch 的依赖追踪原理。我会详细解释原因，并分析两者的区别。</p></blockquote><p><strong>背景：watch 的依赖追踪</strong> 在 Vue 中，watch 的第一个参数是一个“源”（source），它可以是一个函数（返回需要监听的值）、一个 ref、一个响应式对象，或者一个简单的值。watch 会通过这个源的 getter 函数（例如 () =&gt; props 或 () =&gt; ({ ...props })）来追踪依赖，只有当 getter 返回的值发生变化时，才会触发回调。</p><ul><li><strong>props 是一个代理对象</strong>：在 Vue 3 中，props 是通过 reactive 或类似的机制包装的代理对象（Proxy）。它的属性（如 props.value、props.picker）是响应式的，但 props 对象本身的引用是固定的。</li><li><strong>依赖追踪的粒度</strong>：watch 依赖于 getter 函数的返回值，而不是直接监听对象本身。如果 getter 返回的引用没有变化，watch 不会触发回调。</li></ul><p><strong>直接使用 () =&gt; props 的问题</strong> 如果你在 watch 中直接写： typescript CollapseWrapCopy watch( () =&gt; props, (newProps) =&gt; { <em>// 同步逻辑</em> },) 会发生什么？</p><ol><li><strong>props 的引用不变</strong><ul><li>props 是一个固定的代理对象，它的内存地址在组件生命周期内不会改变。即使 props.value 或 props.picker 的值变了，props 本身的引用仍然是同一个。</li><li>() =&gt; props 每次执行都返回同一个对象（同一个引用），而 watch 只关心 getter 返回值的变化。由于引用没变，watch 不会认为有任何变化，也就不会触发回调。</li></ul></li><li><strong>无法检测内部属性的变化</strong><ul><li>watch 默认是浅层监听（shallow watch），它不会深入追踪 props 内部属性的变化（除非显式设置 deep: true）。</li><li>因此，即使 props.value 从 &#39;2023-01-01&#39; 变成了 &#39;2023-02-01&#39;，watch(() =&gt; props, ...) 不会触发，因为它只看到 props 的引用没变。</li></ul></li><li><strong>结果</strong><ul><li>使用 () =&gt; props，watch 回调几乎永远不会执行，除非 props 本身被完全替换（这种情况在 Vue 组件中几乎不可能发生，因为 props 是由父组件传入的代理对象）。</li></ul></li></ol><p><strong>使用 () =&gt; ({ ...props }) 的原因</strong> 现在来看为什么用 () =&gt; ({ ...props })：</p><ol><li><strong>解构创建新对象</strong><ul><li>{ ...props } 使用展开运算符（spread operator）将 props 的所有属性复制到一个新的普通对象中。</li><li>每次 props 的某个属性（如 props.value）发生变化时，{ ...props } 会生成一个新的对象，这个新对象的属性值会反映最新的 props 值。</li><li>因为每次返回的是一个新对象（引用不同），watch 可以检测到变化并触发回调。</li></ul></li><li><strong>依赖追踪生效</strong><ul><li>在 () =&gt; ({ ...props }) 中，Vue 的响应式系统会在执行 getter 时访问 props 的每个属性（如 props.value、props.picker），从而将这些属性注册为依赖。</li><li>当这些属性中的任何一个发生变化时，getter 返回的新对象内容会不同，watch 就会触发。</li></ul></li><li><strong>例子</strong><ul><li>假设 props.value 从 &#39;2023-01-01&#39; 变为 &#39;2023-02-01&#39;： <ul><li>() =&gt; props 返回的始终是同一个 props 引用，watch 不触发。</li><li>() =&gt; ({ ...props }) 返回 { value: &#39;2023-01-01&#39;, ... }，然后变为 { value: &#39;2023-02-01&#39;, ... }，引用变了，watch 触发。</li></ul></li></ul></li></ol><p><strong>是否可以用 deep: true 替代解构？</strong> 你可能会想：如果直接用 () =&gt; props 并加上 { deep: true }，会不会解决问题？ typescript CollapseWrapCopy watch( () =&gt; props, (newProps) =&gt; { <em>// 同步逻辑</em> }, { deep: true },)</p><ul><li><strong>效果</strong>：加上 deep: true，watch 会深度监听 props 的所有属性变化，即使 props 的引用没变，只要内部属性（如 props.value）变了，回调也会触发。</li><li><strong>问题</strong>： <ol><li><strong>性能开销</strong>：深度监听会递归遍历 props 的所有嵌套属性（包括 locale 这样的对象），可能导致不必要的性能消耗，尤其当 props 包含复杂结构时。</li><li><strong>不精确</strong>：你可能只关心某些属性的变化，但 deep 会监听所有属性，包括那些你不希望同步的（如 locale 如果它不常变）。</li></ol></li></ul><p>相比之下，() =&gt; ({ ...props }) 是浅层复制，只关心 props 的第一级属性，既精确又高效。</p><p><strong>为什么不直接用 props（不包函数）？</strong> 你可能会再问：能不能直接写 watch(props, ...) 而不是 () =&gt; props？</p><ul><li><strong>不行</strong>：watch 的第一个参数必须是一个 getter 函数、一个 ref、或一个响应式对象（reactive）。直接传入 props（一个 Proxy 对象）虽然在某些情况下能工作，但 Vue 官方推荐使用 getter 函数来明确依赖。</li><li><strong>原因</strong>：getter 函数允许你控制哪些值被追踪，而直接传入对象可能导致意外的依赖收集或行为不一致。</li></ul><p><strong>总结：为什么要解构？</strong></p><ol><li><strong>() =&gt; props 不行</strong><ul><li>因为 props 的引用不变，watch 无法检测内部属性的变化，除非加 deep: true，但那样不够高效。</li></ul></li><li><strong>() =&gt; ({ ...props }) 的作用</strong><ul><li>解构生成新对象，每次 props 的属性变化时，返回值引用不同，触发 watch。</li><li>依赖追踪精确到 props 的第一级属性，性能和行为都更可控。</li></ul></li><li><strong>为什么不直接用 props 或其他方式？</strong><ul><li>Vue 的响应式机制要求 watch 的源返回可变的值，解构是最简单、直接的办法。</li></ul></li></ol><p><strong>验证实验</strong> 你可以试试以下代码对比效果： typescript CollapseWrapCopy setup(props, { emit }) { <em>// 测试 1：直接用 props</em> watch( () =&gt; props, (newProps) =&gt; { console.log(&#39;props changed:&#39;, newProps) }, ) <em>// 测试 2：用解构</em> watch( () =&gt; ({ ...props }), (newProps) =&gt; { console.log(&#39;props destructured changed:&#39;, newProps) }, ) <em>// 测试 3：加 deep</em> watch( () =&gt; props, (newProps) =&gt; { console.log(&#39;props deep changed:&#39;, newProps) }, { deep: true }, ) <em>// ... 其余代码</em>}</p><ul><li>当 props.value 变化时： <ul><li>测试 1 不触发。</li><li>测试 2 和测试 3 都会触发，但测试 3 的 deep 可能监听过多。</li></ul></li></ul><p><strong>结论</strong> () =&gt; ({ ...props }) 是必要的，因为它通过解构创建新对象，让 watch 能正确检测 props 的变化。直接用 () =&gt; props 不行，除非加 deep: true，但那样会牺牲性能和精确性。解构是目前的最佳实践，既简单又高效，符合 Vue 的响应式设计理念。 如果你还有其他疑问，随时告诉我！</p><p>是的，你提出的这个观察非常正确！在使用 () =&gt; ({ ...props }) 时，即使 props 的属性没有一个值发生变化，watch 仍然会被触发，因为每次 getter 函数执行都会创建一个新的对象引用。让我详细解释一下这个行为，以及它可能带来的影响和解决方法。</p><p><strong>为什么会这样？</strong></p><ol><li><strong>{ ...props } 创建新对象</strong><ul><li>每次执行 () =&gt; ({ ...props })，展开运算符 { ...props } 都会生成一个全新的对象，即使 props 的每个属性值完全相同，新对象的内存地址（引用）都会不同。</li><li>在 JavaScript 中，对象的相等性是基于引用的，而不是内容的。例如： javascriptCollapseWrapCopyconst obj1 = { a: 1 };const obj2 = { a: 1 };console.log(obj1 === obj2); <em>// false，因为引用不同</em></li></ul></li><li><strong>watch 的触发机制</strong><ul><li>Vue 的 watch 默认比较 getter 返回值的引用（浅层比较）。如果 getter 返回的对象引用变了，即使内容完全一样，watch 也会认为值“变了”，从而触发回调。</li><li>在你的例子中，() =&gt; ({ ...props }) 每次返回一个新引用，watch 无法判断内容是否真的变了，因此总是触发。</li></ul></li><li><strong>例子</strong>****假设 props 是 { value: &#39;2023-01-01&#39;, picker: &#39;date&#39; }： <ul><li>第一次执行：{ value: &#39;2023-01-01&#39;, picker: &#39;date&#39; }（引用 A）</li><li>组件重新渲染（即使 props 没变）：{ value: &#39;2023-01-01&#39;, picker: &#39;date&#39; }（引用 B）</li><li>引用 A !== 引用 B，watch 触发。</li></ul></li></ol><p><strong>这会导致什么问题？</strong> 你说得对，即使 props 的属性没有变化，watch 也会执行回调。这可能带来以下影响：</p><ol><li><strong>不必要的执行</strong><ul><li>如果回调中包含同步逻辑（比如更新 datepicker 的状态），这些逻辑会重复运行，即使没有实际变化。</li><li>在你的代码中，Object.assign 或逐个赋值会反复执行，可能导致性能浪费。</li></ul></li><li><strong>潜在的副作用</strong><ul><li>如果回调中有复杂计算或外部操作（比如 API 调用），这种无意义的触发会增加开销。</li></ul></li></ol><p><strong>如何验证？</strong> 你可以用以下代码测试： typescript CollapseWrapCopy setup(props, { emit }) { watch( () =&gt; ({ ...props }), (newProps, oldProps) =&gt; { console.log(&#39;watch triggered&#39;, newProps, oldProps); }, ); <em>// ... 其余代码</em>}</p><ul><li>每次组件渲染（即使 props 没变，比如父组件无关状态更新），watch 都会打印日志，因为新对象引用不同。</li></ul><p><strong>如何解决？</strong> 如果你只想在 props 的内容实际变化时触发 watch，可以采取以下方法： <strong>方案 1：手动比较内容</strong> 在回调中添加内容比较逻辑，只在值变化时执行同步： typescript CollapseWrapCopy setup(props, { emit }) { const datepicker = useDatepicker(<em>/* ... */</em>); watch( () =&gt; ({ ...props }), (newProps, oldProps) =&gt; { <em>// 比较新旧 props 的内容</em> const isChanged = Object.keys(newProps).some( (key) =&gt; newProps[key] !== oldProps[key] ); if (!isChanged) return; <em>// 如果没变，直接返回</em><em>// 只有内容变了才同步</em> datepicker.value.value = newProps.value ?? null; datepicker.currentMonth.value = new Date(newProps.currentMonth); datepicker.picker.value = newProps.picker; <em>// ... 其他同步逻辑</em> }, ); return () =&gt; (props.render ? props.render(datepicker) : DefaultRender(datepicker));}</p><ul><li><strong>优点</strong>：简单，只在内容变化时执行同步逻辑。</li><li><strong>缺点</strong>：需要手动比较，且对深层对象（如 locale）需要额外处理。</li></ul><p><strong>方案 2：使用 deep 并避免解构</strong> 直接监听 props 并启用深层监听，避免每次创建新对象： typescript CollapseWrapCopy watch( () =&gt; props, (newProps) =&gt; { datepicker.value.value = newProps.value ?? null; datepicker.currentMonth.value = new Date(newProps.currentMonth); datepicker.picker.value = newProps.picker; <em>// ... 其他同步逻辑</em> }, { deep: true },);</p><ul><li><strong>优点</strong>：不需要解构，只有 props 的属性实际变化时才会触发。</li><li><strong>缺点</strong>：深层监听可能监听过多（比如 locale 的嵌套属性），性能稍低。</li></ul><p><strong>方案 3：使用自定义比较器</strong> Vue 的 watch 支持自定义 isEqual 函数（在 Vue 3.4+ 中可用），可以精确控制触发条件： typescript CollapseWrapCopy watch( () =&gt; ({ ...props }), (newProps) =&gt; { <em>// 同步逻辑</em> }, { <em>// 自定义比较器</em> flush: &#39;pre&#39;, isEqual: (newVal, oldVal) =&gt; { return Object.keys(newVal).every((key) =&gt; newVal[key] === oldVal[key]); }, },);</p><ul><li><strong>优点</strong>：精确控制，只有内容变化时触发，不需要手动在回调中比较。</li><li><strong>缺点</strong>：需要 Vue 3.4+，且对深层对象仍需额外处理。</li></ul><p><strong>方案 4：优化 useDatepicker 的设计</strong> 更彻底的解决方案是调整 useDatepicker，让它直接接受响应式 props，内部处理同步逻辑，而不是依赖外部 watch： typescript CollapseWrapCopy export function useDatepicker(options: Record&lt;string, any&gt;, emit) { const value = ref(options.value); const currentMonth = ref(options.currentMonth); const picker = ref(options.picker); <em>// ... 其他 ref</em><em>// 内部监听 options 变化</em> watch(() =&gt; options.value, (newVal) =&gt; { value.value = newVal; }); watch(() =&gt; options.currentMonth, (newVal) =&gt; { currentMonth.value = newVal; }); watch(() =&gt; options.picker, (newVal) =&gt; { picker.value = newVal; }); <em>// ... 其他 watch</em><em>// ... 其余逻辑</em>} 然后在组件中直接传入 props： typescript CollapseWrapCopy setup(props, { emit }) { const datepicker = useDatepicker(props, (event, value) =&gt; emit(event, value)); return () =&gt; (props.render ? props.render(datepicker) : DefaultRender(datepicker));}</p><ul><li><strong>优点</strong>：逻辑封装在 useDatepicker 内部，组件代码更简洁，watch 只触发必要变化。</li><li><strong>缺点</strong>：需要修改 useDatepicker，增加其复杂度。</li></ul><p><strong>推荐方案</strong></p><ul><li><strong>短期推荐</strong>：方案 1（手动比较内容），简单且立即可用，适合当前代码。</li><li><strong>长期推荐</strong>：方案 4（优化 useDatepicker），从设计上解决问题，减少外部依赖。</li></ul><p>例如，使用方案 1： typescript CollapseWrapCopy watch( () =&gt; ({ ...props }), (newProps, oldProps) =&gt; { const isChanged = Object.keys(newProps).some( (key) =&gt; newProps[key] !== oldProps[key] ); if (!isChanged) return; datepicker.value.value = newProps.value ?? null; datepicker.currentMonth.value = new Date(newProps.currentMonth); datepicker.picker.value = newProps.picker; <em>// ... 其他同步</em> },); 这样，只有 props 的内容实际变化时，同步逻辑才会执行。</p><p><strong>结论</strong> 是的，() =&gt; ({ ...props }) 会因为新引用导致 watch 每次都触发，即使 props 的值没变。要避免不必要的触发，可以在回调中比较内容，或调整设计让 useDatepicker 内部处理响应性。你的观察非常敏锐，这确实是一个需要注意的细节！有什么其他想法或需求吗？我可以继续帮你优化！</p></div>',1)])])}const h=e(s,[["render",l]]);export{w as __pageData,h as default};
