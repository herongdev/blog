import{c as a,Q as t,j as n,m as e}from"./chunks/framework.B4eEjJBs.js";const g=JSON.parse('{"title":"Electron 里 JS 如何调用系统能力？两套 V8、C++ 绑定与 IPC","description":"","frontmatter":{"title":"Electron 里 JS 如何调用系统能力？两套 V8、C++ 绑定与 IPC","date":"2026-05-19T00:00:00.000Z","tags":["Electron","桌面端","Node.js","V8","IPC"],"categories":["桌面端开发"]},"headers":[],"relativePath":"posts/Electron/JS 如何通过 V8 与 C++ 绑定调用系统能力.md","filePath":"posts/Electron/JS 如何通过 V8 与 C++ 绑定调用系统能力.md","lastUpdated":1779714024000}'),i={name:"posts/Electron/JS 如何通过 V8 与 C++ 绑定调用系统能力.md"};function o(l,s,d,r,p,h){return t(),n("div",null,[...s[0]||(s[0]=[e(`<h2 id="一句话结论" tabindex="-1">一句话结论 <a class="header-anchor" href="#一句话结论" aria-label="Permalink to &quot;一句话结论&quot;">​</a></h2><blockquote><p><strong>主进程里</strong>：JavaScript 通过 V8 调用「启动时已注册的 C++ 函数」，间接使用操作系统能力。<br><strong>渲染进程里（Vue）</strong>：默认<strong>不是</strong>同一个 V8、也<strong>不是</strong>完整 Node；要碰系统得通过 <strong>IPC</strong> 找主进程。</p></blockquote><p>下面按「先分清两套运行时 → 再理解主进程怎么过桥 → 最后看 Vue 怎么间接用能力」的顺序讲。</p><hr><h2 id="一、最容易混的一点-不是同一个-node-也不是同一个-v8" tabindex="-1">一、最容易混的一点：不是同一个 Node，也不是同一个 V8 <a class="header-anchor" href="#一、最容易混的一点-不是同一个-node-也不是同一个-v8" aria-label="Permalink to &quot;一、最容易混的一点：不是同一个 Node，也不是同一个 V8&quot;">​</a></h2><p>很多人以为 <code>main.cjs</code> 和 Vue 打包后的 JS「都在 Electron 里跑，应该是同一个引擎」。<strong>其实不是。</strong></p><h3 id="_1-1-两套-js-跑在两个进程里" tabindex="-1">1.1 两套 JS，跑在两个进程里 <a class="header-anchor" href="#_1-1-两套-js-跑在两个进程里" aria-label="Permalink to &quot;1.1 两套 JS，跑在两个进程里&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────── 主进程 ────────────┐     ┌────────── 渲染进程 ──────────┐</span></span>
<span class="line"><span>│  electron/main.cjs             │     │  Vue 打包后的 JS（dist）        │</span></span>
<span class="line"><span>│  Node 运行时 ✅                 │     │  Node 运行时 ❌（通常已关闭）   │</span></span>
<span class="line"><span>│  require(&#39;electron&#39;) ✅        │     │  更像 Chrome 里跑网页           │</span></span>
<span class="line"><span>│  require(&#39;fs&#39;) ✅              │     │  只有 Web API + 可选 preload   │</span></span>
<span class="line"><span>│  自己的 V8 isolate             │     │  自己的 V8 isolate             │</span></span>
<span class="line"><span>└────────────────────────────────┘     └────────────────────────────────┘</span></span>
<span class="line"><span>              │                                        │</span></span>
<span class="line"><span>              └──────────── IPC（跨进程消息）─────────────┘</span></span></code></pre></div><table tabindex="0"><thead><tr><th>代码</th><th>跑在哪</th><th>环境</th></tr></thead><tbody><tr><td><strong><code>main.cjs</code></strong></td><td>主进程</td><td>完整 <strong>Node + Electron</strong></td></tr><tr><td><strong>Vue 业务代码</strong></td><td><code>BrowserWindow</code> 里的渲染进程</td><td><strong>Chromium</strong>（接近浏览器）</td></tr></tbody></table><p>配置了 <code>nodeIntegration: false</code> 时，Vue 里<strong>不能</strong>像 <code>main.cjs</code> 那样随便 <code>require(&#39;fs&#39;)</code>。</p><h3 id="_1-2-两个-v8-一样吗-——不一样" tabindex="-1">1.2 两个 V8 一样吗？——不一样 <a class="header-anchor" href="#_1-2-两个-v8-一样吗-——不一样" aria-label="Permalink to &quot;1.2 两个 V8 一样吗？——不一样&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>主进程</th><th>渲染进程（Vue）</th></tr></thead><tbody><tr><td>进程</td><td>1 个 Electron 主进程</td><td>每个窗口至少 1 个 Chromium 渲染进程</td></tr><tr><td>V8</td><td>给 <strong>Node</strong> 用的那份</td><td>给 <strong>网页</strong> 用的那份（在 Chromium 里）</td></tr><tr><td>是否同一个堆/上下文</td><td>❌ 隔离的 <strong>两个（或多个）V8 isolate</strong></td><td>❌</td></tr></tbody></table><p>类比：</p><ul><li>主进程 V8 ≈ 后台服务里的 JS 引擎</li><li>渲染进程 V8 ≈ Chrome 打开一个标签页里的 JS 引擎</li></ul><p><strong>不是「一个 V8 跑两份代码」</strong>，而是 <strong>两个进程各有一个 V8</strong>。<br><code>main.cjs</code> 的 JS 和 Vue 的 JS <strong>不会</strong>跟同一个 V8 实例交互。</p><h3 id="_1-3-两个-node-一样吗-——也不一样" tabindex="-1">1.3 两个 Node 一样吗？——也不一样 <a class="header-anchor" href="#_1-3-两个-node-一样吗-——也不一样" aria-label="Permalink to &quot;1.3 两个 Node 一样吗？——也不一样&quot;">​</a></h3><p>更准确地说：<strong>只有主进程那份算「在 Node 里」。</strong></p><ul><li><strong>主进程</strong>：Electron 内置 Node，跑 <code>main.cjs</code>。</li><li><strong>渲染进程</strong>（现代默认配置）：<strong>没有</strong>把完整 Node 暴露给 Vue；页面 JS 由 <strong>Chromium</strong> 执行，环境接近浏览器。</li></ul><blockquote><p>Vue 里的 JS <strong>不是</strong>和 <code>main.cjs</code> 共用同一个 Node 进程里的 Node API。</p></blockquote><h3 id="_1-4-为什么都是-javascript-却感觉像「都在-electron-里」" tabindex="-1">1.4 为什么都是 JavaScript，却感觉像「都在 Electron 里」？ <a class="header-anchor" href="#_1-4-为什么都是-javascript-却感觉像「都在-electron-里」" aria-label="Permalink to &quot;1.4 为什么都是 JavaScript，却感觉像「都在 Electron 里」？&quot;">​</a></h3><p>因为 <strong>Electron 这个桌面应用</strong> 同时带了：</p><ul><li>一套 <strong>Node 主进程</strong>（管窗口、文件、子进程）</li><li>一套 <strong>Chromium</strong>（显示 Vue 页面）</li></ul><p>对外是一个 App，对内是 <strong>多进程</strong>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>electron 可执行文件</span></span>
<span class="line"><span>├── 主进程：Node + V8 + main.cjs</span></span>
<span class="line"><span>└── 渲染进程：Chromium + V8 + Vue（无完整 Node）</span></span></code></pre></div><p><code>window.loadURL(...)</code> 的作用：主进程让 Chromium <strong>另起一个渲染进程</strong> 去加载你的页面。</p><h3 id="_1-5-三个问题-一张表" tabindex="-1">1.5 三个问题，一张表 <a class="header-anchor" href="#_1-5-三个问题-一张表" aria-label="Permalink to &quot;1.5 三个问题，一张表&quot;">​</a></h3><table tabindex="0"><thead><tr><th>问题</th><th>答案</th></tr></thead><tbody><tr><td><code>main.cjs</code> 和 Vue 会跟<strong>同一个 V8</strong> 交互吗？</td><td><strong>不会</strong>，各进程各有一个 V8</td></tr><tr><td>Vue 的 JS 也在 <strong>Node</strong> 里跑吗？</td><td><strong>默认不会</strong>，在 Chromium 渲染进程里跑</td></tr><tr><td>两个 Node 一样吗？</td><td><strong>不一样</strong>；实质是主进程有 Node，Vue 侧没有完整 Node</td></tr></tbody></table><p>后面说的「绑定好了，JS 一调就进 C++」——<strong>对主进程的 <code>main.cjs</code> 成立</strong>；<strong>对 Vue 页面</strong>，默认只有浏览器那套（DOM、<code>fetch</code> 等），要碰系统得 <strong>IPC 找主进程</strong>。</p><hr><h2 id="二、主进程里-js-怎么碰到系统" tabindex="-1">二、主进程里：JS 怎么碰到系统 <a class="header-anchor" href="#二、主进程里-js-怎么碰到系统" aria-label="Permalink to &quot;二、主进程里：JS 怎么碰到系统&quot;">​</a></h2><h3 id="_2-1-调用链路-心智模型" tabindex="-1">2.1 调用链路（心智模型） <a class="header-anchor" href="#_2-1-调用链路-心智模型" aria-label="Permalink to &quot;2.1 调用链路（心智模型）&quot;">​</a></h3><p><strong>不是「JS 被编译成 C++ 指令」</strong>，而是一次跨语言函数调用：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>你写的 JS（require(&#39;fs&#39;).readFile、new BrowserWindow()）</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>V8 执行，遇到「原生绑定」（Native Binding）</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>跳进 Electron / Node 里用 C++ 写好的函数</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>C++ 调操作系统 API</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>结果包装成 JS 值，还给 V8 / 你的代码</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  ┌─────────────┐     原生绑定层      ┌─────────────┐     系统 API     ┌──────────┐</span></span>
<span class="line"><span>  │  你的 JS    │ ─────────────────→ │  C++ 实现   │ ──────────────→ │  操作系统  │</span></span>
<span class="line"><span>  │  (V8 执行)  │ ←───────────────── │ (Node/Electron)│ ←────────────── │          │</span></span>
<span class="line"><span>  └─────────────┘   返回值转成 JS 值   └─────────────┘                   └──────────┘</span></span></code></pre></div><table tabindex="0"><thead><tr><th>层级</th><th>职责</th></tr></thead><tbody><tr><td><strong>V8</strong></td><td>解析、执行 JavaScript</td></tr><tr><td><strong>C++ 绑定层</strong></td><td>注册「JS 函数 ↔ C++ 函数」的桥（<code>fs</code>、<code>BrowserWindow</code>…）</td></tr><tr><td><strong>操作系统</strong></td><td>真正的文件、窗口、进程、网络</td></tr></tbody></table><p>记法：<strong>运行时桥接</strong>，不是 <strong>编译期翻译</strong>。</p><table tabindex="0"><thead><tr><th>说法</th><th>对不对</th></tr></thead><tbody><tr><td>调 <code>readFile</code> 时跳到同进程里的 C++ 函数</td><td>✅</td></tr><tr><td>整份业务 JS 先编译成 C++ 机器码再跑</td><td>❌</td></tr></tbody></table><h3 id="_2-2-事先注册好的「插座」" tabindex="-1">2.2 事先注册好的「插座」 <a class="header-anchor" href="#_2-2-事先注册好的「插座」" aria-label="Permalink to &quot;2.2 事先注册好的「插座」&quot;">​</a></h3><p>C++ 绑定像 <strong>开机时已装好的插座</strong>：</p><ol><li><strong>Electron / Node 启动时</strong>，C++ 向 V8 <strong>注册</strong>原生函数（<code>fs.readFile</code>、<code>BrowserWindow</code>、<code>app.whenReady</code>…）。</li><li>JS 里它们看起来像普通对象、方法。</li><li><strong>一调用</strong>，V8 <strong>直接跳进</strong>已注册的 C++，不会把整段业务 JS「翻译成 C++」。</li><li>C++ 完成后，把返回值变回 JS 类型。</li></ol><blockquote><p><strong>绑定事先写好 → JS 一调 → 就等于执行那段 C++。</strong>（<strong>仅主进程</strong>这条链路。）</p></blockquote><table tabindex="0"><thead><tr><th></th><th>自写 <code>function foo() {}</code></th><th><code>fs.readFile</code> 等原生 API</th></tr></thead><tbody><tr><td>谁执行</td><td>V8 跑 JS</td><td>V8 转调已注册 C++</td></tr><tr><td>能否碰系统</td><td>不能（除非再调别的 API）</td><td>能</td></tr><tr><td>要不要注册</td><td>不需要</td><td><strong>必须</strong></td></tr></tbody></table><p><strong>只有被注册过的 API 才能过桥</strong>；自写函数不会自动变 C++。</p><h3 id="_2-3-主进程示例" tabindex="-1">2.3 主进程示例 <a class="header-anchor" href="#_2-3-主进程示例" aria-label="Permalink to &quot;2.3 主进程示例&quot;">​</a></h3><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.cjs（主进程）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">BrowserWindow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;electron&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">whenReady</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> win</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BrowserWindow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ width: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">800</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, height: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // new BrowserWindow() → C++ 绑定 → 系统创建窗口</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>main.cjs → 主进程 V8 → 命中 C++ 绑定 → 系统 API → 返回 JS 对象</span></span></code></pre></div><p>全程在<strong>主进程一个进程</strong>内完成，对你来说就是「调了一个 JS 函数」。</p><hr><h2 id="三、渲染进程里-vue-怎么间接用系统能力" tabindex="-1">三、渲染进程里：Vue 怎么间接用系统能力 <a class="header-anchor" href="#三、渲染进程里-vue-怎么间接用系统能力" aria-label="Permalink to &quot;三、渲染进程里：Vue 怎么间接用系统能力&quot;">​</a></h2><p>渲染进程里跑的是 <strong>Chromium + Vue</strong>。<code>nodeIntegration: false</code> 时：</p><ul><li>不能随意 <code>require(&#39;fs&#39;)</code>、不能 <code>new BrowserWindow()</code></li><li>页面 JS 由 <strong>渲染进程自己的 V8</strong> 执行，但<strong>没有</strong>主进程那排 Node/Electron「插座」</li></ul><h3 id="_3-1-标准路径-ipc" tabindex="-1">3.1 标准路径：IPC <a class="header-anchor" href="#_3-1-标准路径-ipc" aria-label="Permalink to &quot;3.1 标准路径：IPC&quot;">​</a></h3><p>不能指望「和 main 共用同一个 V8/Node」，而是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Vue（渲染进程 V8）</span></span>
<span class="line"><span>  → ipcRenderer.invoke(&#39;run-agent&#39;, ...)</span></span>
<span class="line"><span>  → 主进程 ipcMain（主进程 V8 + Node）</span></span>
<span class="line"><span>  → fs.readFile / child_process / BrowserWindow …</span></span>
<span class="line"><span>  → 结果经 IPC 回传 Vue</span></span></code></pre></div><table tabindex="0"><thead><tr><th>进程</th><th>能否直接走 C++ 绑定</th><th>典型方式</th></tr></thead><tbody><tr><td>主进程</td><td>✅</td><td><code>require(&#39;electron&#39;)</code>、<code>fs</code></td></tr><tr><td>渲染进程</td><td>❌（默认）</td><td><code>preload</code> + <code>contextBridge</code> + <strong>IPC</strong></td></tr></tbody></table><p><strong>主进程</strong>：同进程函数调用，直接插「插座」。<br><strong>渲染进程</strong>：发消息，请主进程代插「插座」。</p><h3 id="_3-2-preload-窄口子-不是第二套-node" tabindex="-1">3.2 preload：窄口子，不是第二套 Node <a class="header-anchor" href="#_3-2-preload-窄口子-不是第二套-node" aria-label="Permalink to &quot;3.2 preload：窄口子，不是第二套 Node&quot;">​</a></h3><p><strong>preload</strong> 仍在渲染进程，在页面加载前执行，用 <code>contextBridge</code> 向页面暴露<strong>白名单</strong> API（如 <code>window.api.readFile</code>）。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Vue 调用 window.api.readFile(path)</span></span>
<span class="line"><span>  → preload 里封装的 ipcRenderer.invoke</span></span>
<span class="line"><span>  → 主进程 fs.readFile（C++ 绑定 → 系统）</span></span></code></pre></div><p>preload <strong>不会</strong>在渲染进程再注册一套 C++ 绑定；只是把 IPC 包成好用的 JS 函数。</p><hr><h2 id="四、总览图" tabindex="-1">四、总览图 <a class="header-anchor" href="#四、总览图" aria-label="Permalink to &quot;四、总览图&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph renderer [渲染进程：独立 V8，无完整 Node]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Vue[Vue / 页面 JS]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Preload[preload + contextBridge]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Vue --&gt; Preload</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph main [主进程：独立 V8 + Node]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MainJS[main.cjs]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Bindings[启动时注册的 C++ 绑定]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MainJS --&gt; Bindings</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph os [操作系统]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SysAPI[文件 / 窗口 / 进程 API]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Preload --&gt;|IPC| MainJS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Bindings --&gt; SysAPI</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  SysAPI --&gt; Bindings</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Bindings --&gt;|JS 值| MainJS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  MainJS --&gt;|IPC 结果| Preload</span></span></code></pre></div><hr><h2 id="五、自检清单" tabindex="-1">五、自检清单 <a class="header-anchor" href="#五、自检清单" aria-label="Permalink to &quot;五、自检清单&quot;">​</a></h2><p><strong>进程与运行时</strong></p><ul><li>[ ] 能说清主进程、渲染进程是<strong>两个进程、两个 V8</strong></li><li>[ ] 知道 Vue 默认<strong>不在</strong>完整 Node 里跑</li><li>[ ] 知道 <code>main.cjs</code> 与 Vue <strong>不共用</strong>同一个 V8 堆</li></ul><p><strong>主进程与绑定</strong></p><ul><li>[ ] 能说清 V8、C++ 绑定、系统 API 各管什么</li><li>[ ] 知道绑定是<strong>启动时注册</strong>的「插座」，调用时跳进 C++</li><li>[ ] 知道自写 <code>function</code> 不会变 C++；只有已注册 API 能过桥</li><li>[ ] 不会把「JS 调系统」理解成「JS 编译成 C++」</li></ul><p><strong>渲染进程与 IPC</strong></p><ul><li>[ ] 知道渲染进程默认要靠 <strong>IPC</strong> 让主进程调系统 API</li><li>[ ] 知道 preload 是 IPC 的薄封装，不是第二套 Node</li></ul><hr><h2 id="六、小结" tabindex="-1">六、小结 <a class="header-anchor" href="#六、小结" aria-label="Permalink to &quot;六、小结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>维度</th><th>主进程（<code>main.cjs</code>）</th><th>渲染进程（Vue）</th></tr></thead><tbody><tr><td>V8</td><td>主进程里、给 Node 用</td><td>Chromium 里、给网页用</td></tr><tr><td>Node</td><td>✅ 完整 Node + Electron</td><td>❌ 通常关闭 <code>nodeIntegration</code></td></tr><tr><td>调系统</td><td>JS → 已注册 C++ → 系统 API</td><td>IPC → 主进程再调 C++</td></tr><tr><td>记忆</td><td>「插座」在主进程墙上</td><td>通过 IPC 请主进程插电</td></tr></tbody></table><p>学 Electron 桌面端，建议顺序：</p><ol><li>先分清 <strong>两套 V8 / 两套进程</strong>（本章第一节）</li><li>再理解主进程 <strong>注册 + 跳转</strong>（第二节）</li><li>最后看 <strong>IPC、preload、contextIsolation</strong> 如何让 Vue 安全地用主进程能力（第三、四节）</li></ol><p>把这三层串起来，后面读项目里的 <code>main.cjs</code>、<code>preload.ts</code>、Vue 发 IPC 的代码会顺很多。</p>`,77)])])}const k=a(i,[["render",o]]);export{g as __pageData,k as default};
