import{_ as e,o as a,c as t,a5 as n}from"./chunks/framework.DJo0M80U.js";const u=JSON.parse('{"title":"61. System Calls and the Standard Library","description":"The Little Book of C — 61. System Calls and the Standard Library","frontmatter":{"title":"61. System Calls and the Standard Library","date":"2026-07-04","categories":["C 教程"],"tags":["C","Little Book of C","Working Close to the System"],"description":"The Little Book of C — 61. System Calls and the Standard Library","source":"https://little-book-of.github.io/c/books/en-US/book.html","license":"CC BY-NC-SA 4.0","originalAuthor":"Duc-Tam Nguyen","section":61,"sidebarWeight":61,"lang":"en-US","alternateEn":"/posts/c教程/en-US/07-Working Close to the System/061-System Calls and the Standard Library","alternateZh":"/posts/c教程/zh-CN/07-贴近系统/061-System Calls and the Standard Library"},"headers":[],"relativePath":"posts/c教程/en-US/07-Working Close to the System/061-System Calls and the Standard Library.md","filePath":"posts/c教程/en-US/07-Working Close to the System/061-System Calls and the Standard Library.md","lastUpdated":1790163617000}'),l={name:"posts/c教程/en-US/07-Working Close to the System/061-System Calls and the Standard Library.md"};function i(p,s,o,d,c,r){return a(),t("div",null,[...s[0]||(s[0]=[n(`<p>[中文版本](/posts/c教程/zh-CN/07-贴近系统/061-System Calls and the Standard Library)</p><p>When you write C programs that touch files, processes, or devices, you’re talking to the operating system, not directly to hardware. That communication happens through system calls.</p><p>System calls are the lowest-level interface between user-space programs and the OS kernel. C’s standard library (libc) is a thin layer of wrappers built on top of those calls, making them easier to use and more portable.</p><p>Let’s explore how this works, and how to use system calls directly from your C code.</p><h4 id="step-1-what-is-a-system-call" tabindex="-1">Step 1. What Is a System Call? <a class="header-anchor" href="#step-1-what-is-a-system-call" aria-label="Permalink to &quot;Step 1. What Is a System Call?&quot;">​</a></h4><p>A system call (syscall) lets a program request a service from the OS kernel, like reading a file, creating a process, or allocating memory.</p><p>Examples:</p><ul><li><code>read()</code>,<code>write()</code>– access files and devices</li><li><code>fork()</code>,<code>exec()</code>– create and manage processes</li><li><code>open()</code>,<code>close()</code>– handle file descriptors</li><li><code>mmap()</code>– map files into memory</li><li><code>socket()</code>– network communication</li></ul><p>When you call a system function, control passes from user space to kernel space, then back again.</p><h4 id="step-2-the-role-of-libc" tabindex="-1">Step 2. The Role of libc <a class="header-anchor" href="#step-2-the-role-of-libc" aria-label="Permalink to &quot;Step 2. The Role of libc&quot;">​</a></h4><p>The C standard library (glibc, musl, etc.) provides wrappers around these system calls.</p><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;stdio.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    FILE *f = fopen(&quot;test.txt&quot;, &quot;r&quot;);</span></span>
<span class="line"><span>    if (!f) {</span></span>
<span class="line"><span>        perror(&quot;fopen failed&quot;);</span></span>
<span class="line"><span>        return 1;</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    fclose(f);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Under the hood,<code>fopen()</code> eventually calls<code>open()</code>, a system call defined in<code>&lt;fcntl.h&gt;</code>. You can call it directly too.</p><h4 id="step-3-calling-system-calls-directly" tabindex="-1">Step 3. Calling System Calls Directly <a class="header-anchor" href="#step-3-calling-system-calls-directly" aria-label="Permalink to &quot;Step 3. Calling System Calls Directly&quot;">​</a></h4><p>Here’s the same operation without stdio helpers:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;fcntl.h&gt;</span><span>     // open</span></span>
<span class="line"><span>#include &lt;unistd.h&gt;</span><span>    // read, write, close</span></span>
<span class="line"><span>#include &lt;stdio.h&gt;</span><span>     // perror</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    int fd = open(&quot;test.txt&quot;, O_RDONLY);</span></span>
<span class="line"><span>    if (fd == -1) {</span></span>
<span class="line"><span>        perror(&quot;open failed&quot;);</span></span>
<span class="line"><span>        return 1;</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    char buf[128];</span></span>
<span class="line"><span>    ssize_t n = read(fd, buf, sizeof(buf) - 1);</span></span>
<span class="line"><span>    if (n &gt;= 0) {</span></span>
<span class="line"><span>        buf[n] = &#39;\\0&#39;;</span></span>
<span class="line"><span>        write(STDOUT_FILENO, buf, n);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>    close(fd);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Compile and run:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gcc sysread.c -o sysread</span></span>
<span class="line"><span>./sysread</span></span></code></pre></div><p>This prints the first 128 bytes of<code>test.txt</code> directly using system calls, no<code>fopen()</code> or<code>printf()</code> involved.</p><h4 id="tiny-code-minimal-system-call-example" tabindex="-1">Tiny Code: Minimal System Call Example <a class="header-anchor" href="#tiny-code-minimal-system-call-example" aria-label="Permalink to &quot;Tiny Code: Minimal System Call Example&quot;">​</a></h4><p>Let’s drop even the C library and use a raw syscall interface.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;unistd.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    const char msg[] = &quot;Hello via system call\\n&quot;;</span></span>
<span class="line"><span>    write(1, msg, sizeof(msg) - 1);  // 1 = STDOUT</span></span>
<span class="line"><span>    _exit(0);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Compile:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gcc -nostdlib -static syshello.c -o syshello</span></span></code></pre></div><p>Run:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hello via system call</span></span></code></pre></div><p>You just executed a system call directly, bypassing the standard library entirely.</p><h4 id="step-4-file-descriptors" tabindex="-1">Step 4. File Descriptors <a class="header-anchor" href="#step-4-file-descriptors" aria-label="Permalink to &quot;Step 4. File Descriptors&quot;">​</a></h4><p>System calls like<code>read()</code> and<code>write()</code> work with file descriptors, small integer handles managed by the OS.</p><table tabindex="0"><thead><tr><th>Descriptor</th><th>Meaning</th></tr></thead><tbody><tr><td>0</td><td>Standard input (<code>stdin</code>)</td></tr><tr><td>1</td><td>Standard output (<code>stdout</code>)</td></tr><tr><td>2</td><td>Standard error (<code>stderr</code>)</td></tr></tbody></table><p>Every open file, socket, or pipe has a unique descriptor.</p><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>write(1, &quot;Output to stdout\\n&quot;, 17);</span></span>
<span class="line"><span>write(2, &quot;Output to stderr\\n&quot;, 17);</span></span></code></pre></div><h4 id="step-5-inspecting-system-calls" tabindex="-1">Step 5. Inspecting System Calls <a class="header-anchor" href="#step-5-inspecting-system-calls" aria-label="Permalink to &quot;Step 5. Inspecting System Calls&quot;">​</a></h4><p>You can watch your program’s system calls using<code>strace</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>strace ./sysread</span></span></code></pre></div><p>Example output:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>open(&quot;test.txt&quot;, O_RDONLY) = 3</span></span>
<span class="line"><span>read(3, &quot;Hello World\\n&quot;, 12) = 12</span></span>
<span class="line"><span>write(1, &quot;Hello World\\n&quot;, 12) = 12</span></span>
<span class="line"><span>close(3) = 0</span></span></code></pre></div><p>This shows the real kernel-level operations, a great debugging and learning tool.</p><h4 id="step-6-return-values-and-errors" tabindex="-1">Step 6. Return Values and Errors <a class="header-anchor" href="#step-6-return-values-and-errors" aria-label="Permalink to &quot;Step 6. Return Values and Errors&quot;">​</a></h4><p>System calls usually return:</p><ul><li>≥ 0 → success (bytes read, process ID, etc.)</li><li>-1 → error (with<code>errno</code> set to an error code)</li></ul><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;errno.h&gt;</span></span>
<span class="line"><span>#include &lt;string.h&gt;</span></span>
<span class="line"><span>int fd = open(&quot;missing.txt&quot;, O_RDONLY);</span></span>
<span class="line"><span>if (fd == -1)</span></span>
<span class="line"><span>    fprintf(stderr, &quot;Error: %s\\n&quot;, strerror(errno));</span></span></code></pre></div><p>Common error codes:</p><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>ENOENT</code></td><td>File not found</td></tr><tr><td><code>EACCES</code></td><td>Permission denied</td></tr><tr><td><code>EBADF</code></td><td>Invalid descriptor</td></tr><tr><td><code>EINTR</code></td><td>Interrupted system call</td></tr></tbody></table><h4 id="step-7-mixing-system-calls-and-stdio" tabindex="-1">Step 7. Mixing System Calls and stdio <a class="header-anchor" href="#step-7-mixing-system-calls-and-stdio" aria-label="Permalink to &quot;Step 7. Mixing System Calls and stdio&quot;">​</a></h4><p>You can combine both layers safely, just don’t mix them on the same file descriptor.</p><p>Example (safe):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FILE *f = fopen(&quot;log.txt&quot;, &quot;w&quot;);</span></span>
<span class="line"><span>write(STDOUT_FILENO, &quot;Console log\\n&quot;, 12);</span></span>
<span class="line"><span>fprintf(f, &quot;File log\\n&quot;);</span></span>
<span class="line"><span>fclose(f);</span></span></code></pre></div><p>Example (unsafe):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FILE *f = fopen(&quot;data.txt&quot;, &quot;w&quot;);</span></span>
<span class="line"><span>write(fileno(f), &quot;mixed output\\n&quot;, 13);  // may confuse buffering</span></span></code></pre></div><h4 id="step-8-system-call-wrappers" tabindex="-1">Step 8. System Call Wrappers <a class="header-anchor" href="#step-8-system-call-wrappers" aria-label="Permalink to &quot;Step 8. System Call Wrappers&quot;">​</a></h4><p>The Linux kernel provides hundreds of system calls. You can call most through<code>&lt;unistd.h&gt;</code>, but for rare ones, you can use<code>syscall()</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;sys/syscall.h&gt;</span></span>
<span class="line"><span>#include &lt;unistd.h&gt;</span></span>
<span class="line"><span>int main(void) {</span></span>
<span class="line"><span>    syscall(SYS_write, 1, &quot;Hello syscall\\n&quot;, 14);</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div><h4 id="step-9-viewing-available-syscalls" tabindex="-1">Step 9. Viewing Available Syscalls <a class="header-anchor" href="#step-9-viewing-available-syscalls" aria-label="Permalink to &quot;Step 9. Viewing Available Syscalls&quot;">​</a></h4><p>Check all available system calls for your platform:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>man 2 intro</span></span></code></pre></div><p>or</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ausyscall --dump | head</span></span></code></pre></div><p>You’ll see a list like:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>read, write, open, close, stat, fork, execve, mmap, ...</span></span></code></pre></div><h4 id="step-10-why-it-matters" tabindex="-1">Step 10. Why It Matters <a class="header-anchor" href="#step-10-why-it-matters" aria-label="Permalink to &quot;Step 10. Why It Matters&quot;">​</a></h4><p>System calls are the foundation of everything in C and Unix-like systems.</p><p>They give your program direct access to:</p><ul><li>Files and devices</li><li>Processes and signals</li><li>Memory and networking</li><li>Time and environment</li></ul><p>Learning to use them directly is essential for understanding how higher-level abstractions (like<code>stdio</code>,<code>pthreads</code>, or sockets) are built.</p><h4 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h4><ol><li>Use<code>strace</code> on<code>ls</code> or<code>cat</code> to see how system calls drive them.</li><li>Replace a<code>fopen()</code>/<code>fread()</code> pair with direct<code>open()</code> and<code>read()</code> calls.</li><li>Write a tiny file copier using only<code>open</code>,<code>read</code>,<code>write</code>, and<code>close</code>.</li><li>Experiment with invalid file descriptors and print out<code>errno</code>.</li><li>Build a version of<code>echo</code> that uses only raw system calls.</li></ol><p>Next, you’ll take this a step further: learning how to create and manage processes with<code>fork()</code> and<code>exec()</code>, the heart of Unix multitasking.</p>`,71)])])}const m=e(l,[["render",i]]);export{u as __pageData,m as default};
