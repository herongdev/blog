import{_ as t,o,c as a,a5 as i}from"./chunks/framework.DJo0M80U.js";const d=JSON.parse('{"title":"Optimizing Javascript for fun and for profit","description":"Optimizing Javascript for fun and for profit I often feel like javascript code in general runs much slower than it could, simply because it’。","frontmatter":{"title":"Optimizing Javascript for fun and for profit","date":"2026-08-11T00:00:00.000Z","categories":["JavaScript 系统教程"],"tags":["JavaScript","前端","教程","OneNote","实战与性能"],"description":"Optimizing Javascript for fun and for profit I often feel like javascript code in general runs much slower than it could, simply because it’。","sidebarWeight":15,"lastUpdated":false,"feed":false,"source":"onenote","sourceNote":"OneNote/b-原生js/性能优化/Optimizing Javascript for fun and for profit/Optimizing Javascript for fun and for profit.md"},"headers":[],"relativePath":"posts/JavaScript系统教程/08-实战与性能/Optimizing Javascript for fun and for profit/Optimizing Javascript for fun and for profit.md","filePath":"posts/JavaScript系统教程/08-实战与性能/Optimizing Javascript for fun and for profit/Optimizing Javascript for fun and for profit.md"}'),s={name:"posts/JavaScript系统教程/08-实战与性能/Optimizing Javascript for fun and for profit/Optimizing Javascript for fun and for profit.md"};function n(r,e,l,c,h,p){return o(),a("div",null,[...e[0]||(e[0]=[i('<div><h1 id="optimizing-javascript-for-fun-and-for-profit" tabindex="-1">Optimizing Javascript for fun and for profit <a class="header-anchor" href="#optimizing-javascript-for-fun-and-for-profit" aria-label="Permalink to &quot;Optimizing Javascript for fun and for profit&quot;">​</a></h1><blockquote><p>本节目标：理解“Optimizing Javascript for fun and for profit”的核心思路，并能把它用于实际开发或面试表达。 Optimizing Javascript for fun and for profit I often feel like javascript code in general runs much slower than it could, simply because it’s not optimized properly. Here is a summary of common optimization techniques I’ve found useful. Note that the tradeoff for performance is often readability, so the question of when to go for performance versus readability is a question left to the reader. I’ll also note that talking about optimization necessarily requires talking about benchmarking. Micro-optimizing a function for hours to have it run 100x faster is meaningless if the function only represented a fraction of the actual overall runtime to start with. If one is optimizing, the first and most important step is benchmarking. I’ll cover the topic in the later points. Be also aware that micro-benchmarks are often flawed, and that may include those presented here. I’ve done my best to avoid those traps, but don’t blindly apply any of the points presented here without benchmarking.</p></blockquote><p>I have included runnable examples for all cases where it’s possible. They show by default the results I got on my machine (brave 122 on archlinux) but you can run them yourself. As much as I hate to say it, Firefox has fallen a bit behind in the optimization game, and represents a very small fraction of the traffic for now, so I don’t recommend using the results you’d get on Firefox as useful indicators.</p><ol start="0"><li><p>Avoid work This might sound evident, but it needs to be here because there can’t be another first step to optimization: if you’re trying to optimize, you should first look into avoiding work. This includes concepts like memoization, laziness and incremental computation. This would be applied differently depending on the context. In React, for example, that would mean applying memo(), useMemo() and other applicable primitives.</p></li><li><p>Avoid string comparisons Javascript makes it easy to hide the real cost of string comparisons. If you need to compare strings in C, you’d use the strcmp(a, b) function. Javascript uses === instead, so you don’t see the strcmp. But it’s there, and a string comparison will usually (but not always) require comparing each of the characters in the string with the ones in the other string; string comparison is O(n). One common JavaScript pattern to avoid is strings-as-enums. But with the advent of TypeScript this should be easily avoidable, as enums are integers by default.</p></li></ol><p>// No enum Position { TOP = &#39;TOP&#39;, BOTTOM = &#39;BOTTOM&#39;, } // Yeppers enum Position { TOP, // = 0 BOTTOM, // = 1 } Here is a comparison of the costs:</p><p>// 1. string compare const Position = { TOP: &#39;TOP&#39;, BOTTOM: &#39;BOTTOM&#39;, } let _ = 0 for (let i = 0; i &lt; 1000000; i++) { let current = i % 2 === 0 ? Position.TOP : Position.BOTTOM if (current === Position.TOP) _ += 1 } // 2. int compare const Position = { TOP: 0, BOTTOM: 1, } let _ = 0 for (let i = 0; i &lt; 1000000; i++) { let current = i % 2 === 0 ? Position.TOP : Position.BOTTOM if (current === Position.TOP) _ += 1 }</p><ol><li>string compare: 50.35%</li><li>int compare: 100% Run About benchmarks Percentage results represent the number of operations completed within 1s, divided by the number of operations of the highest scoring case. Higher is better.</li></ol><p>As you can see, the difference can be significant. The difference isn’t necessarily due to the strcmp cost as engines can sometimes use a string pool and compare by reference, but it’s also due to the fact that integers are usually passed by value in JS engines, whereas strings are always passed as pointers, and memory accesses are expensive (see section 5). In string-heavy code, this can have a huge impact.</p><p>For a real-world example, I was able to make this JSON5 javascript parser run 2x faster* just by replacing string constants with numbers. *Unfortunately it wasn’t merged, but that’s how open-source is.</p><ol start="2"><li>Avoid different shapes Javascript engines try to optimize code by assuming that objects have a specific shape, and that functions will receive objects of the same shape. This allows them to store the keys of the shape once for all objects of that shape, and the values in a separate flat array. To represent it in javascript:</li></ol><p>const objects = [ { name: &#39;Anthony&#39;, age: 36, }, { name: &#39;Eckhart&#39;, age: 42 }, ] const shape = [ { name: &#39;name&#39;, type: &#39;string&#39; }, { name: &#39;age&#39;, type: &#39;integer&#39; }, ] const objects = [ [&#39;Anthony&#39;, 36], [&#39;Eckhart&#39;, 42], ] A note on terminology I have used the word “shape” for this concept, but be aware that you may also find “hidden class” or “map” used to describe it, depending on the engine.</p><p>For example, at runtime if the following function receives two objects with the shape { x: number, y: number }, the engine is going to speculate that future objects will have the same shape, and generate machine code optimized for that shape.</p><p>function add(a, b) { return { x: a.x + b.x, y: a.y + b.y, } } If one would instead pass an object not with the shape { x, y } but with the shape { y, x }, the engine would need to undo its speculation and the function would suddenly become considerably slower. I’m going to limit my explanation here because you should read the excellent post from mraleph if you want more details, but I’m going to highlight that V8 in particular has 3 modes, for accesses that are: monomorphic (1 shape), polymorphic (2-4 shapes), and megamorphic (5+ shapes). Let’s say you really want to stay monomorphic, because the slowdown is drastic:</p><p>// setup let _ = 0 // 1. monomorphic const o1 = { a: 1, b: _, c: _, d: _, e: _ } const o2 = { a: 1, b: _, c: _, d: _, e: _ } const o3 = { a: 1, b: _, c: _, d: _, e: _ } const o4 = { a: 1, b: _, c: _, d: _, e: _ } const o5 = { a: 1, b: _, c: _, d: _, e: _ } // all shapes are equal // 2. polymorphic const o1 = { a: 1, b: _, c: _, d: _, e: _ } const o2 = { a: 1, b: _, c: _, d: _, e: _ } const o3 = { a: 1, b: _, c: _, d: _, e: _ } const o4 = { a: 1, b: _, c: _, d: _, e: _ } const o5 = { b: _, a: 1, c: _, d: _, e: _ } // this shape is different // 3. megamorphic const o1 = { a: 1, b: _, c: _, d: _, e: _ } const o2 = { b: _, a: 1, c: _, d: _, e: _ } const o3 = { b: _, c: _, a: 1, d: _, e: _ } const o4 = { b: _, c: _, d: _, a: 1, e: _ } const o5 = { b: _, c: _, d: _, e: _, a: 1 } // all shapes are different // test case function add(a1, b1) { return a1.a + a1.b + a1.c + a1.d + a1.e + b1.a + b1.b + b1.c + b1.d + b1.e } let result = 0 for (let i = 0; i &lt; 1000000; i++) { result += add(o1, o2) result += add(o3, o4) result += add(o4, o5) }</p><ol><li>monomorphic: 100%</li><li>polymorphic: 13.07%</li><li>megamorphic: 4.09% Run What the eff should I do about this? Easier said than done but: create all your objects with the exact same shape. Even something as trivial as writing your React component props in a different order can trigger this.</li></ol><p>For example, here are simple cases I found in React’s codebase, but they already had a much higher impact case of the same problem a few years ago because they were initializing an object with an integer, then later storing a float. Yes, changing the type also changes the shape. Yes, there are integer and float types hidden behind number. Deal with it.</p><p>Number representation Engines can usually encode integers as values. For example, V8 represents values in 32 bits, with integers as compact Smi (SMall Integer) values, but floats and large integers are passed as pointers just like strings and objects. JSC uses a 64 bit encoding, double tagging, to pass all numbers by value, as SpiderMonkey does, and the rest is passed as pointers.</p><ol start="3"><li>Avoid array/object methods I love functional programming as much as anyone else, but unless you’re working in Haskell/OCaml/Rust where functional code gets compiled to efficient machine code, functional will always be slower than imperative.</li></ol><p>const result = [1.5, 3.5, 5.0] .map(n =&gt; Math.round(n)) .filter(n =&gt; n % 2 === 0) .reduce((a, n) =&gt; a + n, 0) The problem with those methods is that:</p><p>They need to make a full copy of the array, and those copies later need to be freed by the garbage collector. We will explore more in details the issues of memory I/O in section 5. They loop N times for N operations, whereas a for loop would allow looping once. // setup: const numbers = Array.from({ length: 10_000 }).map(() =&gt; Math.random()) // 1. functional const result = numbers .map(n =&gt; Math.round(n * 10)) .filter(n =&gt; n % 2 === 0) .reduce((a, n) =&gt; a + n, 0) // 2. imperative let result = 0 for (let i = 0; i &lt; numbers.length; i++) { let n = Math.round(numbers[i] * 10) if (n % 2 !== 0) continue result = result + n }</p><ol><li>functional: 36.51%</li><li>imperative: 100% Run</li></ol><p>Object methods such as Object.values(), Object.keys() and Object.entries() suffer from similar problems, as they also allocate more data, and memory accesses are the root of all performance issues. No really I swear, I’ll show you in section 5.</p><ol start="4"><li>Avoid indirection Another place to look for optimization gains is any source of indirection, of which I can see 3 main sources:</li></ol><p>const point = { x: 10, y: 20 } // 1. // Proxy objects are harder to optimize because their get/set function might // be running custom logic, so engines can&#39;t make their usual assumptions. const proxy = new Proxy(point, { get: (t, k) =&gt; { return t[k] } }) // Some engines can make proxy costs disappear, but those optimizations are // expensive to make and can break easily. const x = proxy.x // 2. // Usually ignored, but accessing an object via <code>.</code> or <code>[]</code> is also an // indirection. In easy cases, the engine may very well be able to optimize the // cost away: const x = point.x // But each additional access multiplies the cost, and makes it harder for the // engine to make assumptions about the state of <code>point</code>: const x = this.state.circle.center.point.x // 3. // And finally, function calls can also have a cost. Engine are generally good // at inlining these: function getX(p) { return p.x } const x = getX(p) // But it&#39;s not guaranteed that they can. In particular if the function call // isn&#39;t from a static function but comes from e.g. an argument: function Component({ point, getX }) { return getX(point) } The proxy benchmark is particularly brutal on V8 at the moment. Last time I checked, proxy objects were always falling back from the JIT to the interpreter, seeing from those results it might still be the case.</p><p>// 1. proxy access const point = new Proxy({ x: 10, y: 20 }, { get: (t, k) =&gt; t[k] }) for (let _ = 0, i = 0; i &lt; 100_000; i++) { _ += point.x } // 2. direct access const point = { x: 10, y: 20 } const x = point.x for (let _ = 0, i = 0; i &lt; 100_000; i++) { _ += x }</p><ol><li>proxy access: 0%</li><li>direct access: 0% Show I also wanted to showcase accessing a deeply nested object vs direct access, but engines are very good at optimizing away object accesses via escape analysis when there is a hot loop and a constant object. I inserted a bit of indirection to prevent it.</li></ol><p>// 1. nested access const a = { state: { center: { point: { x: 10, y: 20 } } } } const b = { state: { center: { point: { x: 10, y: 20 } } } } const get = (i) =&gt; i % 2 ? a : b let result = 0 for (let i = 0; i &lt; 100_000; i++) { result = result + get(i).state.center.point.x } // 2. direct access const a = { x: 10, y: 20 }.x const b = { x: 10, y: 20 }.x const get = (i) =&gt; i % 2 ? a : b let result = 0 for (let i = 0; i &lt; 100_000; i++) { result = result + get(i) }</p><ol><li>nested access: 0%</li><li>direct access: 0% Show</li><li>Avoid cache misses This point requires a bit of low-level knowledge, but has implications even in javascript, so I’ll explain. From the CPU point of view, retrieving memory from RAM is slow. To speed things up, it uses mainly two optimizations.</li></ol><p>5.1 Prefetching The first one is prefetching: it fetches more memory ahead of time, in the hope that it’s the memory you’ll be interested in. It always guesses that if you request one memory address, you’ll be interested in the memory region that comes right after that. So accessing data sequentially is the key. In the following example, we can observe the impact of accessing memory in random order.</p><p>// setup: const K = 1024 const length = 1 * K * K // Theses points are created one after the other, so they are allocated // sequentially in memory. const points = new Array(length) for (let i = 0; i &lt; points.length; i++) { points[i] = { x: 42, y: 0 } } // This array contains the <em>same data</em> as above, but shuffled randomly. const shuffledPoints = shuffle(points.slice()) // 1. sequential let _ = 0 for (let i = 0; i &lt; points.length; i++) { _ += points[i].x } // 2. random let _ = 0 for (let i = 0; i &lt; shuffledPoints.length; i++) { _ += shuffledPoints[i].x }</p><ol><li>sequential: 0%</li><li>random: 0% Show What the eff should I do about this? This aspect is probably the hardest to put in practice, because javascript doesn’t have a way of placing objects in memory, but you can use that knowledge to your advantage as in the example above, for example to operate on data before re-ordering or sorting it. You cannot assume that objects created sequentially will stay at the same location after some time because the garbage collector might move them around. There is one exception to that, and it’s arrays of numbers, preferably TypedArray instances:</li></ol><p>// from this const points = [{ x: 0, y: 5 }, { x: 0, y: 10 }] // to this const points = new Int64Array([0, 5, 0, 10]) For a more detailed example, see this link* . *Note that it contains some optimizations that are now outdated, but it’s still accurate overall.</p><p>5.2 Caching in L1/2/3 The second optimization CPUs use is the L1/L2/L3 caches: those are like faster RAMs, but they are also more expensive, so they are much smaller. They contain RAM data, but act as an LRU cache. Data comes in while it’s “hot” (being worked on), and is written back to the main RAM when new working data needs the space. So the key here is use as little data as possible to keep your working dataset in the fast caches. In the following example, we can observe what are the effects of busting each of the successive caches.</p><p>// setup: const KB = 1024 const MB = 1024 * KB // These are approximate sizes to fit in those caches. If you don&#39;t get the // same results on your machine, it might be because your sizes differ. const L1 = 256 * KB const L2 = 5 * MB const L3 = 18 * MB const RAM = 32 * MB // We&#39;ll be accessing the same buffer for all test cases, but we&#39;ll // only be accessing the first 0 to <code>L1</code> entries in the first case, // 0 to <code>L2</code> in the second, etc. const buffer = new Int8Array(RAM) buffer.fill(42) const random = (max) =&gt; Math.floor(Math.random() * max) // 1. L1 let r = 0; for (let i = 0; i &lt; 100000; i++) { r += buffer[random(L1)] } // 2. L2 let r = 0; for (let i = 0; i &lt; 100000; i++) { r += buffer[random(L2)] } // 3. L3 let r = 0; for (let i = 0; i &lt; 100000; i++) { r += buffer[random(L3)] } // 4. RAM let r = 0; for (let i = 0; i &lt; 100000; i++) { r += buffer[random(RAM)] }</p><ol><li>L1: 0%</li><li>L2: 0%</li><li>L3: 0%</li><li>RAM: 0% Show What the eff should I do about this? Ruthlessly eliminate every single data or memory allocations that can be eliminated. The smaller your dataset is, the faster your program will run. Memory I/O is the bottleneck for 95% of programs. Another good strategy can be to split your work into chunks, and ensure you work on a small dataset at a time.</li></ol><p>For more details on CPU and memory, see this link.</p><p>About immutable data structures Immutability is great for clarity and correctness, but in the context of performance, updating an immutable data structure means making a copy of the container, and that’s more memory I/O that will flush your caches. You should avoid immutable data structures when possible.</p><p>About the { ...spread } operator It’s very convenient, but every time you use it you create a new object in memory. More memory I/O, slower caches!</p><ol start="6"><li>Avoid large objects As explained in section 2, engines use shapes to optimize objects. However, when the shape grows too large, the engine has no choice but to use a regular hashmap (like a Map object). And as we saw in section 5, cache misses decrease performance significantly. Hashmaps are prone to this because their data is usually randomly &amp; evenly distributed over the memory region they occupy. Let’s see how it behaves with this map of some users indexed by their ID.</li></ol><p>// setup: const USERS_LENGTH = 1_000 // setup: const byId = {} Array.from({ length: USERS_LENGTH }).forEach((_, id) =&gt; { byId[id] = { id, name: &#39;John&#39;} }) let _ = 0 // 1. [] access Object.keys(byId).forEach(id =&gt; { _ += byId[id].id }) // 2. direct access Object.values(byId).forEach(user =&gt; { _ += user.id })</p><ol><li>[] access: 0%</li><li>direct access: 0% Show And we can also observe how the performance keeps degrading as the object size grows:</li></ol><p>// setup: const USERS_LENGTH = 100_000</p><ol><li><p>[] access: 0%</p></li><li><p>direct access: 0% Show What the eff should I do about this? As demonstrated above, avoid having to frequently index into large objects. Prefer turning the object into an array beforehand. Organizing your data to have the ID on the model can help, as you can use Object.values() and not have to refer to the key map to get the ID.</p></li><li><p>Use eval Some javascript patterns are hard to optimize for engines, and by using eval() or its derivatives you can make those patterns disappear. In this example, we can observe how using eval() avoids the cost of creating an object with a dynamic object key:</p></li></ol><p>// setup: const key = &#39;requestId&#39; const values = Array.from({ length: 100_000 }).fill(42) // 1. without eval function createMessages(key, values) { const messages = [] for (let i = 0; i &lt; values.length; i++) { messages.push({ [key]: values[i] }) } return messages } createMessages(key, values) // 2. with eval function createMessages(key, values) { const messages = [] const createMessage = new Function(&#39;value&#39;, <code>return { ${JSON.stringify(key)}: value }</code> ) for (let i = 0; i &lt; values.length; i++) { messages.push(createMessage(values[i])) } return messages } createMessages(key, values)</p><ol><li>without eval: 0%</li><li>with eval: 0% Show Another good use-case for eval could be to compile a filter predicate function where you discard the branches that you know will never be taken. In general, any function that is going to be run in a very hot loop is a good candidate for this kind of optimization.</li></ol><p>Obviously the usual warnings about eval() apply: don’t trust user input, sanitize anything that gets passed into the eval()‘d code, and don’t create any XSS possibility. Also note that some environments don’t allow access to eval(), such as browser pages with a CSP.</p><ol start="8"><li>Use strings, carefully We’ve already seen above how strings are more expensive than they appear. Well I have kind of a good news/bad news situation here, which I’ll announce in the only logical order (bad first, good second): strings are more complex than they appear, but they can also be quite efficient used well.</li></ol><p>String operations are a core part of JavaScript due to its context. To optimize string-heavy code, engines had to be creative. And by that I mean, they had to represent the String object with multiple string representation in C++, depending on the use case. There are two general cases you should worry about, because they hold true for V8 (the most common engine by far), and generally also in other engines.</p><p>First, strings concatenated with + don’t create a copy of the two input strings. The operation creates a pointer to each substring. If it was in typescript, it would be something like this:</p><p>class String { abstract value(): char[] {} } class BytesString { constructor(bytes: char[]) { this.bytes = bytes } value() { return this.bytes } } class ConcatenatedString { constructor(left: String, right: String) { this.left = left this.right = right } value() { return [...this.left.value(), ...this.right.value()] } } function concat(left, right) { return new ConcatenatedString(left, right) } const first = new BytesString([&#39;H&#39;, &#39;e&#39;, &#39;l&#39;, &#39;l&#39;, &#39;o&#39;, &#39; &#39;]) const second = new BytesString([&#39;w&#39;, &#39;o&#39;, &#39;r&#39;, &#39;l&#39;, &#39;d&#39;]) // See ma, no array copies! const message = concat(first, second) Second, string slices also don’t need to create copies: they can simply point to a range in another string. To continue with the example above:</p><p>class SlicedString { constructor(source: String, start: number, end: number) { this.source = source this.start = start this.end = end } value() { return this.source.value().slice(this.start, this.end) } } function substring(source, start, end) { return new SlicedString(source, start, end) } // This represents &quot;He&quot;, but it still contains no array copies. // It&#39;s a SlicedString to a ConcatenatedString to two BytesString const firstTwoLetters = substring(message, 0, 2) But here’s the issue: once you need to start mutating those bytes, that’s the moment you start paying copy costs. Let’s say we go back to our String class and try to add a .trimEnd method:</p><p>class String { abstract value(): char[] {} trimEnd() { // <code>.value()</code> here might be calling // our Sliced-&gt;Concatenated-&gt;2*Bytes string! const bytes = this.value() const result = bytes.slice() while (result[result.length - 1] === &#39; &#39;) result.pop() return new BytesString(result) } } So let’s jump to an example where we compare using operations that use mutation versus only using concatenation:</p><p>// setup: const classNames = [&#39;primary&#39;, &#39;selected&#39;, &#39;active&#39;, &#39;medium&#39;] // 1. mutation const result = classNames .map(c =&gt; <code>button--${c}</code>) .join(&#39; &#39;) // 2. concatenation const result = classNames .map(c =&gt; &#39;button--&#39; + c) .reduce((acc, c) =&gt; acc + &#39; &#39; + c, &#39;&#39;)</p><ol><li>mutation: 0%</li><li>concatenation: 0% Show What the eff should I do about this? In general, try to avoid mutation for as long as possible. This includes methods such as .trim(), .replace(), etc. Consider how you can avoid those methods. In some engines, string templates can also be slower than +. In V8 at the moment it’s the case, but might not be in the future so as always, benchmark.</li></ol><p>A note on SlicedString above, you should note that if a small substring to a very large string is alive in memory, it might prevent the garbage collector from collecting the large string! If you’re processing large texts and extracting small strings from it, you might be leaking large amounts of memory.</p><p>const large = Array.from({ length: 10_000 }).map(() =&gt; &#39;string&#39;).join(&#39;&#39;) const small = large.slice(0, 50) // ^ will keep <code>large</code> alive The solution here is to use mutation methods to our advantage. If we use one of them on small, it will force a copy, and the old pointer to large will be lost:</p><p>// replace a token that doesn&#39;t exist const small = small.replace(&#39;#&#39;.repeat(small.length + 1), &#39;&#39;) For more details, see string.h on V8 or JSString.h on JavaScriptCore.</p><p>On strings complexity I have skimmed very quickly over things, but there are a lot of implementation details that add complexity to strings. There are often minimum lengths for each of those string representations. For example a concatenated string might not be used for very small strings. Or sometimes there are limits, for example avoiding pointing to a substring of a substring. Reading the C++ files linked above gives a good overview of the implementation details, even if just reading the comments.</p><ol start="9"><li>Use specialization One important concept in performance optimization is specialization: adapting your logic to fit in the constraints of your particular use-case. This usually means figuring out what conditions are likely to be true for your case, and coding for those conditions.</li></ol><p>Let’s say we are a merchant that sometimes needs to add tags to their product list. We know from experience that our tags are usually empty. Knowing that information, we can specialize our function for that case:</p><p>// setup: const descriptions = [&#39;apples&#39;, &#39;oranges&#39;, &#39;bananas&#39;, &#39;seven&#39;] const someTags = { apples: &#39;::promotion::&#39;, } const noTags = {} // Turn the products into a string, with their tags if applicable function productsToString(description, tags) { let result = &#39;&#39; description.forEach(product =&gt; { result += product if (tags[product]) result += tags[product] result += &#39;, &#39; }) return result } // Specialize it now function productsToStringSpecialized(description, tags) { // We know that <code>tags</code> is likely to be empty, so we check // once ahead of time, and then we can remove the <code>if</code> check // from the inner loop if (isEmpty(tags)) { let result = &#39;&#39; description.forEach(product =&gt; { result += product + &#39;, &#39; }) return result } else { let result = &#39;&#39; description.forEach(product =&gt; { result += product if (tags[product]) result += tags[product] result += &#39;, &#39; }) return result } } function isEmpty(o) { for (let _ in o) { return false } return true } // 1. not specialized for (let i = 0; i &lt; 100; i++) { productsToString(descriptions, someTags) productsToString(descriptions, noTags) productsToString(descriptions, noTags) productsToString(descriptions, noTags) productsToString(descriptions, noTags) } // 2. specialized for (let i = 0; i &lt; 100; i++) { productsToStringSpecialized(descriptions, someTags) productsToStringSpecialized(descriptions, noTags) productsToStringSpecialized(descriptions, noTags) productsToStringSpecialized(descriptions, noTags) productsToStringSpecialized(descriptions, noTags) }</p><ol><li>not specialized: 0%</li><li>specialized: 0% Show This sort of optimization can give you moderate improvements, but those will add up. They are a nice addition to more crucial optimizations, like shapes and memory I/O. But note that specialization can turn against you if your conditions change, so be careful when applying this one.</li></ol><p>Branch prediction and branchless code Removing branches from your code can be incredibly efficient for performance. For more details on what a branch predictor is, read the classic Stack Overflow answer Why is processing a sorted array faster.</p><ol start="10"><li>Data structures I won’t go in details about data structures as they would require their own post. But be aware that using the incorrect data structures for your use-case can have a bigger impact than any of the optimizations above. I would suggest you to be familiar with the native ones like Map and Set, and to learn about linked lists, priority queues, trees (RB and B+) and tries.</li></ol><p>But for a quick example, let’s compare how Array.includes does against Set.has for a small list:</p><p>// setup: const userIds = Array.from({ length: 1_000 }).map((_, i) =&gt; i) const adminIdsArray = userIds.slice(0, 10) const adminIdsSet = new Set(adminIdsArray) // 1. Array let _ = 0 for (let i = 0; i &lt; userIds.length; i++) { if (adminIdsArray.includes(userIds[i])) { _ += 1 } } // 2. Set let _ = 0 for (let i = 0; i &lt; userIds.length; i++) { if (adminIdsSet.has(userIds[i])) { _ += 1 } }</p><ol><li>Array: 0%</li><li>Set: 0% Show As you can see, the data structure choice makes a very impactful difference.</li></ol><p>As a real-world example, I had a case where we were able to reduce the runtime of a function from 5 seconds to 22 milliseconds by switching out an array with a linked list.</p><ol start="11"><li>Benchmarking I’ve left this section for the end for one reason: I needed to establish credibility with the fun sections above. Now that I (hopefully) have it, let me tell you that benchmarking is the most important part of optimization. Not only is it the most important, but it’s also hard. Even after 20 years of experience, I still sometimes create benchmarks that are flawed, or use the profiling tools incorrectly. So whatever you do, please put the most effort into benchmarking correctly.</li></ol><p>11.0 Start with the top Your priority should always be to optimize the function/section of code that makes up the biggest part of your runtime. If you spend time optimizing anything else than the top, you are wasting time.</p><p>11.1 Avoid micro-benchmarks Run your code in production mode and base your optimizations on those observations. JS engines are very complex, and will often behave differently in micro-benchmarks than in real-world scenarios. For example, take this micro-benchmark:</p><p>const a = { type: &#39;div&#39;, count: 5, } const b = { type: &#39;span&#39;, count: 10 } function typeEquals(a, b) { return a.type === b.type } for (let i = 0; i &lt; 100_000; i++) { typeEquals(a, b) } If you’ve payed attention sooner, you will realize that the engine will specialize the function for the shape { type: string, count: number }. But does that hold true in your real-world use-case? Are a and b always of that shape, or will you receive any kind of shape? If you receive many shapes in production, this function will behave differently then.</p><p>11.2 Doubt your results If you’ve just optimized a function and it now runs 100x faster, doubt it. Try to disprove your results, try it in production mode, throw stuff at it. Similarly, doubt also your tools. The mere fact of observing a benchmark with devtools can modify its behavior.</p><p>11.3 Pick your target Different engines will optimize certain patterns better or worse than others. You should benchmark for the engine(s) that are relevant to you, and prioritize which one is more important. Here’s a real-world example in Babel where improving V8 means decreasing JSC’s performance.</p><ol start="12"><li>Profiling &amp; tools Various remarks about profiling and devtools.</li></ol><p>12.1 Browser gotchas If you’re profiling in the browser, make sure you use a clean and empty browser profile. I even use a separate browser for this. If you’re profiling and you have browser extensions enabled, they can mess up the measurements. React devtools in particular will substantially affect results, rendering code may appear slower than it appears in the mirror to your users.</p><p>12.2 Sample vs structural profiling Browser profiling tools are sample-based profilers, which take a sample of your stack at regular intervals. This had a big disadvantage: very small but very frequent functions might be called between those samples, and might be very much underreported in the stack charts you’ll get. Use Firefox devtools with a custom sample interval or Chrome devtools with CPU throttling to mitigate this issue.</p><p>12.3 The tools of the trade Beyond the regular browser devtools, it may help to be aware of these options:</p><p>Chrome devtools have quite a few experimental flags that can help you figure out why things are slow. The style invalidation tracker is invaluable when you need to debug style/layout recalculations in the browser. <a href="https://github.com/iamakulov/devtools-perf-features" target="_blank" rel="noreferrer">https://github.com/iamakulov/devtools-perf-features</a></p><p>The deoptexplorer-vscode extension allows you to load V8/chromium log files to understand when your code is triggering deoptimizations, such as when you pass different shapes to a function. You don’t need the extension to read log files, but it makes the experience much more pleasant. <a href="https://github.com/microsoft/deoptexplorer-vscode" target="_blank" rel="noreferrer">https://github.com/microsoft/deoptexplorer-vscode</a></p><p>You can always compile the debug shell for each JS engine to understand more in details how it works. This allows you to run perf and other low-level tools, and also to inspect the bytecode and machine code generated by each engine. Example for V8 | Example for JSC | Example for SpiderMonkey (missing)</p><p>Final notes Hope you learned some useful tricks. If you have any comments, corrections or questions, email in the footer. I’m always happy to receive feedback or questions from readers.</p></div>',1)])])}const m=t(s,[["render",n]]);export{d as __pageData,m as default};
