import{_ as t,o,c as p,a5 as c}from"./chunks/framework.DJo0M80U.js";const l=JSON.parse('{"title":"封装","description":"","frontmatter":{"title":"封装","date":"2026-07-03T00:00:00.000Z","categories":["Java 快速入门"],"tags":["Java","面向对象","OneNote"],"lastUpdated":false},"headers":[],"relativePath":"posts/java快速入门/第二部分：面向对象/01-类与对象/封装/封装.md","filePath":"posts/java快速入门/第二部分：面向对象/01-类与对象/封装/封装.md"}'),a={name:"posts/java快速入门/第二部分：面向对象/01-类与对象/封装/封装.md"};function d(n,e,s,i,r,g){return o(),p("div",null,[...e[0]||(e[0]=[c("<div><p>面向对象编程语言是对客观世界的模拟，客观世界里成员变量都是隐藏在对象内部的，外界无法直接操作和修改。 封装可以被认为是一个保护屏障，防止该类的代码和数据被其他类随意访问。要访问该类的数据，必须通过指定的方式。适当的封装可以让代码更容易理解与维护，也加强了代码的安全性。</p><p><strong>原则</strong> 将<strong>属性隐藏</strong>起来，若需要访问某个属性，<strong>提供公共方法</strong>对其访问。</p><p><strong>封装的步骤</strong><code>1.</code> 使用 <code>private</code> 关键字来修饰成员变量。</p><p><code>2.</code></p><p>对需要访问的成员变量，提供对应的一对 <code>getXxx</code> 方法 、 <code>setXxx</code> 方法。</p><p><strong>封装的操作——</strong><code>private</code><strong>关键字</strong><code>private</code><strong>的含义</strong></p><p><code>1. private</code></p><p>是一个权限修饰符，代表最小权限。</p><p><code>2.</code></p><p>可以修饰成员变量和成员方法。</p><p><code>3.</code></p><p>被<code>private</code>修饰后的成员变量和成员方法，只在本类中才能访问。</p><p><code>private</code><strong>的使用格式</strong></p><p><code>private</code></p><p>数据类型 变量名 ；</p><p><code>1.</code></p><p>使用 <code>private</code> 修饰成员变量，代码如下：</p><p>public class Student { private String name; private int age; } 2. 提供 <code>getXxx</code> 方法 <code>/ setXxx</code> 方法，可以访问成员变量，代码如下：</p><p>public class Student { private String name; private int age; public void setName(String n) { name = n; } public String getName() { return name; } public void setAge(int a) { age = a; } public int getAge() { return age; } } <strong>封装优化</strong><code>1</code><strong>——</strong><code>this</code><strong>关键字</strong> 我们发现 <code>setXxx</code> 方法中的形参名字并不符合见名知意的规定，那么如果修改与成员变量名一致，是否就见名知意了呢？代码如下：</p><p>public class Student { private String name; private int age; public void setName(String name) { name = name; } public void setAge(int age) { age = age; } } 经过修改和测试，我们发现新的问题，成员变量赋值失败了。也就是说，在修改了 <code>setXxx()</code> 的形参变量名后，方法并没有给成员变量赋值！这是由于形参变量名与成员变量名重名，导致成员变量名被隐藏，方法中的变量名，无法访问到成员变量，从而赋值失败。所以，我们只能使用<code>this</code>关键字，来解决这个重名问题。</p><p><code>this</code><strong>的含义</strong></p><p><code>this</code></p><p>代表所在类的当前对象的引用（地址值），即对象自己的引用。 记住 ：方法被哪个对象调用，方法中的<code>this</code>就代表那个对象。即谁在调用，<code>this</code>就代表谁。</p><p><code>this</code><strong>使用格式</strong></p><p><code>this.</code></p><p>成员变量名； 使用 <code>this</code> 修饰方法中的变量，解决成员变量被隐藏的问题，代码如下：</p><p>public class Student { private String name; private int age; public void setName(String name) { //name = name; this.name = name; } public String getName() { return name; } public void setAge(int age) { //age = age; this.age = age; } public int getAge() { return age; } } 小贴士：方法中只有一个变量名时，默认也是使用 <code>this</code> 修饰，可以省略不写。</p><p><strong>封装优化</strong><code>2</code><strong>——构造方法</strong> 当一个对象被创建时候，构造方法用来初始化该对象，给对象的成员变量赋初始值。 小贴士：无论你与否自定义构造方法，所有的类都有构造方法，因为<code>Java</code>自动提供了一个无参数构造方法， 一旦自己定义了构造方法，<code>Java</code>自动提供的默认无参数构造方法就会失效。</p><p><strong>构造方法的定义格式</strong> 修饰符 构造方法名<code>(</code>参数列表</p><p>){ // 方法体</p><p>} 构造方法的写法上，方法名与它所在的类名相同。它没有返回值，所以不需要返回值类型，甚至不需要<code>void</code>。使用构造方法后，代码如下：</p><p>public class Student { private String name; private int age; // 无参数构造方法</p><p>public Student() {} // 有参数构造方法</p><p>public Student(String name,int age) { this.name = name; this.age = age; } } <strong>注意事项</strong></p><p><code>1.</code></p><p>如果你不提供构造方法，系统会给出无参数构造方法。</p><p><code>2.</code></p><p>如果你提供了构造方法，系统将不再提供无参数构造方法。</p><p><code>3.</code></p><p>构造方法是可以重载的，既可以定义参数，也可以不定义参数。</p></div>",1)])])}const m=t(a,[["render",d]]);export{l as __pageData,m as default};
