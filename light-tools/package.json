{
  "name": "light-tools",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter @light-tools/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
