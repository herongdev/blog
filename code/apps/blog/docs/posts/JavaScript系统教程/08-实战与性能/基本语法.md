---
title: "基本语法"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "实战与性能"
description: "标识符中的字母也可以包含扩展的ASCII或Unicode字母字符，但我们不推荐这样做。 标识符命名规范： ECMAScript标识符采用驼峰命名，也就是标识符的第一个字母小写，剩下的每个单词的首字母大写。 注释 单行注释：单行注释以两个斜杠开头 多行注释：以一个斜杠和一个星号（/。"
sidebarWeight: 38
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/规范/基本语法.md"
---
::: v-pre

# 基本语法

> 本节目标：理解“基本语法”的核心思路，并能把它用于实际开发或面试表达。

> 说明：原 OneNote 中有图片引用，但图片未包含在导出目录中；本页保留了可用的文字与代码内容。
```
ECMA-262通过叫做ECMAScript的“伪语言”描述了 JavaScript的所有这些基本概念。
```

```
ECMAScript的语法大量借鉴了C及其他类C语言（如Java和Peri）的语法。
```

```
**区分大小写**
ECMAScript中的一切（变量、函数名和操作符）都区分大小写。
```

```
**标识符**
指变量、函数、属性或函数的参数的名字。
命名规定：
```

```
标识符是一个或多个字符；
```

- ```
    第一个字符只能是
    ```

    ```
    字母
    ```

    ```
    或者下划线（_）
    ```

    ```
    或者美元符号（$）；
    ```

```
后面的字符可以是字母、下划线、美元符号或数字。
```

标识符中的字母也可以包含扩展的ASCII或Unicode字母字符，但我们不推荐这样做。

```
不能把关键字、保留字等用作标识符。
```

**标识符命名规范：**
ECMAScript标识符采用驼峰命名，也就是标识符的第一个字母小写，剩下的每个单词的首字母大写。

**注释**
单行注释：单行注释以两个斜杠开头
多行注释：以一个斜杠和一个星号（/* ）开头，以一个星号和一个斜杠（*/ ）结尾；多行注释可以用在同一行作单行注释用。多行示例如下：

第二和第三行都开头的星号不是必需的，纯粹是为了提高注释的可读性。

**严格模式****strict mode**
为JavaScript定义了一种不同的解析与执行模型。
作用：作为向标准语言的过渡
特点：对某些不安全的操作会抛出错误。JavaScript的执行结果会有很大不同；
启用：在所有代码之前添加代码："use strict"。它是一个编译指示（pragma），用于告诉支持的JavaScript引擎切换到严格模式。
函数内使用：在函数内部第一行添加编译指示，也可以指定函数在严格模式下执行，因此本书将会随时指出严格模式下的区别。支

**关键字**
具有特定用途的单词，不能用作标识符。全部关键字（带*号上标的是第5版新增的关键字）如下：

**保留字**
保留字暂时没有任何特定的用途，但它们有可能在将来被用作关键字。以下是ECMA-262第3版定义的全部保留字：

```
第5版把在非严格模式下运行时的保留字缩减为下列这些:
```

```
在严格模式下，第5版还对以下保留字施加了限制：
```

```
注意，
```

```
let和yield是第5版新增的保留字；
```

ES5的严格模式下，eval和arguments不能作为标识符或属性名，否则会抛出错误。

ES5中关键字和保留字可以用作对象的属性名，但最好不要这样做。

在实现ECMAScript3的JavaScript引擎中使用关键字作标识符，会导致"Identifier Expected"错误。而使用保留字作标识符可能会也可能不会导致相同的错误，具体取决于特定的引擎。

:::
