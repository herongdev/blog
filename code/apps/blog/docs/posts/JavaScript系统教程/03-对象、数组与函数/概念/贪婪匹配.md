---
title: "贪婪匹配"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "对象、数组与函数"
description: "重复类量词都具有贪婪性，在条件允许的前提下，会匹配尽可能多的字符。 示例 1 下面示例显示当多个重复类量词同时满足条件时，会在保证右侧重复类量词最低匹配次数基础上，使最左侧的重复类量词尽可能占有所有字符。"
sidebarWeight: 128
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/引用数据类型-正则/概念/贪婪匹配.md"
---
::: v-pre

# 贪婪匹配

> 本节目标：理解“贪婪匹配”的核心思路，并能把它用于实际开发或面试表达。
重复类量词都具有贪婪性，在条件允许的前提下，会匹配尽可能多的字符。

```
?、{n} 和 {n,m} 重复类具有弱贪婪性，表现为贪婪的有限性。
```

```
*、+ 和 {n,} 重复类具有强贪婪性，表现为贪婪的无限性。
```

```
越是排在左侧的重复类量词匹配优先级越高
```

**示例****1**
下面示例显示当多个重复类量词同时满足条件时，会在保证右侧重复类量词最低匹配次数基础上，使最左侧的重复类量词尽可能占有所有字符。

```
取匹配右表达式的最短的字符串；
```

```
取匹配左表达式的最长字符串；
```

```
**var** s = "<html><head><title></title></head><body></body></html>";
**var** r = /(<.*>)(<.*>)/
**var** a = s.match(r);
//左侧表达式匹配"<html><head><title></title></head><body></body></html>"
console.log(a[1])；
console.log(a[2]);  //右侧表达式匹配“</html>”
```

:::
