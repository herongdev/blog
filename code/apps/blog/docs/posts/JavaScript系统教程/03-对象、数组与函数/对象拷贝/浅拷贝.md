---
title: "浅拷贝"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "对象、数组与函数"
description: "浅拷贝 特点 简单类型 Undefined ， Null ， Boolean ， Number 和 String 是存入堆，直接引用； object array 则是存入栈中，通过复制指针（内存地址）来引用值；如果拷贝后的对象发生变化，原对象也会发生变化。"
sidebarWeight: 184
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/面向对象程序设计/对象拷贝/浅拷贝.md"
---
::: v-pre

# 浅拷贝

> 本节目标：理解“浅拷贝”的核心思路，并能把它用于实际开发或面试表达。
**浅拷贝****特点**

- 简单类型`Undefined`，`Null`，`Boolean`，`Number`和`String`是存入堆，直接引用；
- `object array` 则是存入栈中，通过复制指针（内存地址）来引用值；如果拷贝后的对象发生变化，原对象也会发生变化。

```
function shallowCopy(object) {
    // 只拷贝对象
    if (!object || typeof object !== "object") return;
    // 根据 object 的类型判断是新建一个数组还是对象
    let newObject = Array.isArray(object) ? [] : {};
    // 遍历 object，并且判断是 object 的属性才拷贝
    for (let key in object) {
        if (object.hasOwnProperty(key)) {
            newObject[key] = object[key];
        }
    }
    return newObject;
}
```

:::
