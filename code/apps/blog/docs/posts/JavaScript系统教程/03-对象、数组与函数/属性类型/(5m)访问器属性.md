---
title: "访问器属性"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "对象、数组与函数"
description: "访问器属性不能直接定义，必须使用 Object.defineProperty() 来定义 访问器属性不包含数据值； 它们包含一对儿 getter 和 setter 函数。 在读取访问器属性时，会调用 getter 函数，这个函数负责返回有效的值； 在写入访问器属性时，会调用 se。"
sidebarWeight: 189
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/面向对象程序设计/属性类型/(5m)访问器属性.md"
---
::: v-pre

# 访问器属性

> 本节目标：理解“访问器属性”的核心思路，并能把它用于实际开发或面试表达。
访问器属性不能直接定义，必须使用`Object.defineProperty()`来定义

访问器属性不包含数据值；
它们包含一对儿`getter`和`setter`函数。

在读取访问器属性时，会调用`getter`函数，这个函数负责返回有效的值；
在写入访问器属性时，会调用`setter`函数，这个函数负责决定如何处理数据。

不一定非要同时指定`getter`和`setter`。
只指定`getter`意味着属性不能写，尝试写入属性会被忽略，严格模式下会报错。
只指定`setter`意味着属性不能读，尝试读取属性会返回`undefined`，严格模式下会报错。

访问器属性有如下`4`个特性：
`[[Configurable]]`：表示

1. 能否通过`delete`删除属性，从而重新定义属性；
2. 能否修改属性的特性，如`Configurable`**和**`Enumerable`；
3. 能否把属性修改为数据属性`;`
4. 直接在对象上定义的属性，默认值为`true`。使用`Object.defineProperty()`方法定义的访问器属性此值默认为`false`。

`[[Enumerable]]`**：**表示：

- 能否通过`for-in`循环返回属性；

`[[Get]]`：在读取属性时调用的函数。默认值为`undefined`。
`[[Set]]`**：**在写入属性时调用的函数。默认值为`undefined`。

:::
