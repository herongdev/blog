---
title: "中间件定义"
date: 2026-08-11
categories:
  - "React 系统教程"
tags:
  - "React"
  - "Redux"
  - "前端"
  - "教程"
  - "OneNote"
  - "状态管理与路由"
description: "新建一个中间件 dispatch }) 调用下一个中间件， next 等于 store 的原始 dispatch 方法 return function 这个就是改造后的 dispatch return function { if action { return } return。"
sidebarWeight: 40
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/redux/级联中间件 /中间件定义.md"
---
::: v-pre

# 中间件定义

> 本节目标：理解“中间件定义”的核心思路，并能把它用于实际开发或面试表达。
新建一个中间件

```
Src/redux-thunk.js
function
```

```
thunk({
```

```
getState,
```

 `dispatch` `})`

```
{
//
```

调用下一个中间件，`next`等于`store`的原始`dispatch`方法
  `return` `function`

```
(next)
```

```
{
   //
```

这个就是改造后的`dispatch`
    `return` `function`

```
(action)
```

 `{`
      `if`

```
(typeof
```

 `action` `===`

```
'function')
```

 `{`
        `return`

```
action(dispatch,
```

```
getState);
```
       `}`
      `return`

```
next(action);
```
     `}`

```
}
}
export
```

 `default`

```
thunk;
```

```
Src/redux-logger.js
function
```

```
logger({
```

```
getState,
```

 `dispatch` `})`

```
{//dispatch
```

指提改造后的`dispatch`
  `return` `function`

```
(next)
```

 `{`
    `return` `function`

```
(action)
```

 `{`

```
console.log('prev
```

```
state',
```

```
getState());
```

```
next(action);
```

```
console.log('next
```

```
state',
```

```
getState());
```
     `}`

```
}
}
export
```

 `default`

```
logger;
```

```
Src/redux-promise.js
/**
```
  `*` `dispatch` 重新派发
 `*` `next` 下一步
 `*` `@param` `{*}` `param0`
 `*` `@returns`

```
*/
function
```

```
promise({
```

```
getState,
```

 `dispatch` `})` `{`
  `return` `function`

```
(next)
```

 `{`
    `return` `function`

```
(action)
```

 `{`
      `if`

```
(action.then
```

 `&&` `typeof`

```
action.then
```

 `===`

```
'function')
```

 `{`

```
action.then(dispatch).catch(dispatch);
```
       `}` `else` `if`

```
(action.payload
```

 `&&` `typeof`

```
action.payload.then
```

 `===`

```
'function')
```

 `{`

```
action.payload
```

```
.then(payload
```

 `=\>`

```
dispatch({
```

```
...action,
```

 `payload` `}))`

```
.catch(error
```

 `=\>` `{`

```
dispatch({
```

```
...action,
```

 `error:`

```
true,
```

 `payload:` `error` `});`
            `return`

```
Promise.reject(error);
```
           `})`
      `}` `else` `{`

```
next(action);
```
       `}`
    `}`

```
}
}
export
```

 `default`

```
promise;
```

:::
