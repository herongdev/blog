---
title: "严格模式"
date: 2026-08-11
categories:
  - "Vue 系统教程"
tags:
  - "Vue"
  - "Vue3"
  - "前端"
  - "教程"
  - "OneNote"
  - "路由与状态管理"
description: "开启严格模式，仅需在创建 store 的时候传入 strict: true ： 在严格模式下，无论何时发生了状态变更且不是由 mutation 函数引起的，将会抛出错误。这能保证所有的状态变更都能被调试工具跟踪到。 开发环境与发布环境 不要在发布环境下启用严格模式！严格模式会深度。"
sidebarWeight: 91
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/vuex/严格模式.md"
---
::: v-pre

# 严格模式

> 本节目标：理解“严格模式”的核心思路，并能把它用于实际开发或面试表达。
开启严格模式，仅需在创建 `store` 的时候传入 `strict: true`：

```
const store = new Vuex.Store({  // ...  strict: true})
```
 在严格模式下，无论何时发生了状态变更且不是由 `mutation` 函数引起的，将会抛出错误。这能保证所有的状态变更都能被调试工具跟踪到。

**开发环境与发布环境**
不要在发布环境下启用严格模式！严格模式会深度监测状态树来检测不合规的状态变更——请确保在发布环境下关闭严格模式，以避免性能损失。
类似于插件，我们可以让构建工具来处理这种情况：

```
const store = new Vuex.Store({  // ...  strict: process.env.NODE_ENV !== 'production'})
```
 \> 来自

```
 <https://vuex.vuejs.org/zh/guide/strict.html>
```

:::
