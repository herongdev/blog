---
title: "数据渲染"
date: 2026-08-11
categories:
  - "Vue 系统教程"
tags:
  - "Vue"
  - "Vue3"
  - "前端"
  - "教程"
  - "OneNote"
  - "原理与手写实现"
description: "要将模块最终转换为虚拟dom，因为虚拟dom为普通js对象，这样我们就可以进行diff对比，针对性更新dom. 渲染定制的地方，其中render优先级最高，template其次，el元素最低 template属性： render方法： el元素： 可以使用el指定容器，也可以使用。"
sidebarWeight: 75
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/手写/数据渲染/数据渲染.md"
---
::: v-pre

# 数据渲染

> 本节目标：理解“数据渲染”的核心思路，并能把它用于实际开发或面试表达。

> 说明：原 OneNote 中有图片引用，但图片未包含在导出目录中；本页保留了可用的文字与代码内容。
要将模块最终转换为虚拟dom，因为虚拟dom为普通js对象，这样我们就可以进行diff对比，针对性更新dom.

渲染定制的地方，其中render优先级最高，template其次，el元素最低
template属性：
render方法：
el元素：

可以使用el指定容器，也可以使用$mount方法；
其本质上是一样的。

:::
