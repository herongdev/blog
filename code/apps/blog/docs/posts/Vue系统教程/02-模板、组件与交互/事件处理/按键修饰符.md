---
title: "按键修饰符"
date: 2026-08-11
categories:
  - "Vue 系统教程"
tags:
  - "Vue"
  - "Vue3"
  - "前端"
  - "教程"
  - "OneNote"
  - "模板、组件与交互"
description: "在监听键盘事件时，我们经常需要检查详细的按键。 Vue 允许为 v on 在监听键盘事件时添加按键修饰符： \\<! 只有在 key 是 Enter 时调用 你可以直接将 暴露的任意有效按键名转换为 kebab case 来作为修饰符。 在上述示例中，处理函数只会在 $event.。"
sidebarWeight: 31
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/api/事件处理/按键修饰符.md"
---
::: v-pre

# 按键修饰符

> 本节目标：理解“按键修饰符”的核心思路，并能把它用于实际开发或面试表达。
==在监听键盘事件时，我们经常需要检查详细的按键。==`Vue` ==允许为== `v-on` ==在监听键盘事件时添加按键修饰符：==
`\<!--` ==只有在== `` `key` `` ==是== `` `Enter` `` ==时调用==

```
 `vm.submit()` --><input v-on:keyup.enter="submit">
```
 ==你可以直接将==

```
KeyboardEvent.key
```

 ==暴露的任意有效按键名转换为== `kebab-case` ==来作为修饰符。==

```
<input v-on:keyup.page-down="onPageDown">
```
 ==在上述示例中，处理函数只会在== `$event.key` ==等于== `PageDown` ==时被调用。==

:::
