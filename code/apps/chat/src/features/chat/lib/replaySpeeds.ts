/** Equally spaced slider stops map to actual local replay multipliers. */
export const replaySpeeds = [1, 1.5, 2, 3, 4] as const;
export const replaySpeedLabels = [
  "standard",
  "brisk",
  "fast",
  "faster",
  "fastest",
] as const;
