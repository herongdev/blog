---
description: Git 提交信息使用简体中文，简洁规范
alwaysApply: true
---

# Git 提交规范

- 提交说明使用**简体中文**，**简洁**，符合团队规范。
- 格式：`<类型>: <简要说明>`（如 `docs:`、`feat:`、`fix:`、`chore:`）。
- 说清做了什么或为什么，不堆砌文件名，不用 `update` / `WIP` 等模糊描述。
- 仅在用户**明确要求**时执行 `git commit`；不提交敏感文件。

示例：

```text
docs: 补充 TypeScript declare global 与三斜线指令说明
feat: 接入顶栏 logo 与首页 favicon
fix: 修正 sitemap 的 SITE_URL 配置
```
