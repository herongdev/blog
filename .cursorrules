# 博客项目 Cursor 规则

## Git 提交信息（必须遵守）

- **语言**：提交说明一律使用**简体中文**。
- **风格**：简洁、规范、说清「做了什么 / 为什么」，避免空话和堆砌文件名。
- **长度**：优先 **1 行**；必要时补充 1～2 行正文，每行不宜过长。
- **时机**：仅在用户明确要求时执行 `git commit`；不要主动提交。

### 推荐格式

```text
<类型>: <简要说明>
```

常用类型：

| 类型 | 用途 |
|------|------|
| `feat` | 新功能、新页面、新能力 |
| `fix` | 修复 bug 或错误行为 |
| `docs` | 文档、博客文章 |
| `style` | 样式、UI 调整（不改逻辑） |
| `refactor` | 重构（不改对外行为） |
| `chore` | 构建、配置、依赖、脚本 |
| `ci` | CI/CD 相关 |

### 示例

```text
docs: 补充 Bing 收录与 IndexNow 实操教程
feat: 接入站点 logo 与 favicon
fix: 修正 VitePress 页脚备案链接显示
chore: 更新 GitHub Actions 部署环境变量
```

### 避免

- 英文提交说明（除非用户明确要求）
- `update`、`fix bug`、`WIP`、无意义描述
- 一次 commit 混入无关改动
- 提交 `.env`、密钥、Token 等敏感文件

### 提交前

- 确认改动与提交说明一致
- 遵循仓库既有 hook，失败则修复后**新 commit**，不要随意 `--amend`（除非用户要求且符合安全条件）
