---
title: "生命周期"
date: 2026-08-11
categories:
  - "React 系统教程"
tags:
  - "React"
  - "Redux"
  - "前端"
  - "教程"
  - "OneNote"
  - "核心概念与组件"
description: "生命周期，就是一个对象从开始生成到最后消亡所经历的状态，理解生命周期，是合理开发的关键。 3. React 16 主要变化： 1. 向下兼容的 Fiber ，防止了客户端 react 在进行渲染的时候阻塞页面的其他交互行为。 2. React 16.4 ， componentWi。"
sidebarWeight: 77
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/概念/生命周期/生命周期.md"
---
::: v-pre

# 生命周期

> 本节目标：理解“生命周期”的核心思路，并能把它用于实际开发或面试表达。

> 说明：原 OneNote 中有图片引用，但图片未包含在导出目录中；本页保留了可用的文字与代码内容。
生命周期，就是一个对象从开始生成到最后消亡所经历的状态，理解生命周期，是合理开发的关键。

```
组件生命周期大致分三个阶段：
```

```
首次绘制阶段：初始化和加载；
```

```
运行交互阶段：处理交互、接收事件并更新界面；
```

3. ```
    卸载==销毁==阶段：做一些清理工作。
    ```

    ```
    settimeInterval,settimeout；
    ```

    ```
    还有一些事件处理函数；
    ```

`React 16`主要变化：

1. 向下兼容的`Fiber`，防止了客户端`react`在进行渲染的时候阻塞页面的其他交互行为。
2. `React 16.4`，`componentWillReceiveProps`这个历史遗留即将被`deprecated`，

**新的生命周期过程**

变化在于`+2` 生命周期，`-3 UNSAFE`。
==新增：==

- `getDerivedStateFromProps`==，==
- `getSnapshotBeforeUpdate`

`UNSAFE`==：==

- `UNSAFE_componentWillMount`==；==
- `UNSAFE_componentWillUpdate`==；==
- `UNSAFE_componentWillReceiveProps`==。==

:::
