---
title: 更好的属性注入方式1
date: 2026-07-03
categories: [Java 快速入门]
tags: [Java, Spring, OneNote]
lastUpdated: false
---
::: v-pre

```
这种方式适用于属性配置文件在多个地方用到
一、将之前的properties文件改名为application.properties
二、新建一个类文件，用来保存属性值JdbcProperties：
三、引入依赖，lombok将为我们类自动生成getter,setter等方法，是一个编译式依赖。
==\<dependency\>==
==\<groupId\>====org.projectlombok====\</groupId\>==
==\<artifactId\>====lombok====\</artifactId\>==
==\</dependency\>==
便可以类上使用Data注解来自动生成以下方法；


`四、定义类成员变量，用来保存properties`



五、修改JdbcConfig类的内容，去掉@resoure和@value注解
直接引入JdbcProperties中定义的属性


总体是用一个类从properties中引入属性，然后在@configuration的java配置类中，把这个类传去，这样属性类和配置类分离。

错误解决：如果lombok不生效，可能是没有在idea中安装此插件，在设置-\>plugins中下载安装，再重启即可。
```

:::
