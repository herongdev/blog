---
title: 内存管理
date: 2026-07-03
categories: [Java 快速入门]
tags: [Java, 面向对象, OneNote]
---
```
在Java中，内存管理是动态的、自动的。当创建新对象时，Java自动为该对象分配适当数量的内存，不用显式地为对象分配内存。这项工作是由Java虚拟机（JVM）完成。

程序运行时，Java虚拟机定期地查找未用的对象，并收回这些对象占用的内存。这被称为动态垃圾收集，它是完全自动的。不必显式地释放对象占用的内存；只需要确保不再占用要删除的对象。这是Java明显优于其前身C++的地方之一。
```
