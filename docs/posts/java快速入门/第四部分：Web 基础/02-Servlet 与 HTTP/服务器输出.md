---
title: 服务器输出
date: 2026-07-03
categories: [Java 快速入门]
tags: [Java, Web基础, OneNote]
---
`1.`

服务器输出字符数据到浏览器
`*` 步骤：
`1.` 获取字符输出流
`2.` 输出数据
`*` 注意乱码问题：
`1. PrintWriter pw = response.getWriter();`获取的流的默认编码是

- ISO-8859-1
- 2.

设置该流的默认编码
`3.` 告诉浏览器响应体使用的编码
`//`简单的形式，设置编码，是在获取流之前设置
`// response.setCharacterEncodeing("utf-8");`只设置了流的代码，没告诉浏览器服务器所用的编码，浏览器不会按指定编码显示；
`//` 告诉浏览器，服务器发送的消息体数据的编码。建议浏览器使用该编码

response.setHeader("content-type","text/html;charset=utf-8");
 简写为：
`response.setContentType("text/html;charset=utf-8");`

`2.`

服务器输出字节数据到浏览器
`*` 步骤：
`1.` 获取字节输出流

ServletOutputStream sos = response.getOutputStread();
sos.write("
您好`".getBytes("utf-8"));`// 文本一般用字符流
`2.` 输出数据

`3.`

验证码
`1.` 本质：图片
`2.` 目的：防止恶意表单注册
