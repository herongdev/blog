---
title: "Date.UTC()"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "对象、数组与函数"
description: "这个方法同样是根据传入的参数，返回一个表示日期的毫秒数。 传给这个方法的参数，按顺序依次表示年份，基于0开始的月份，月中的哪一天，小时数（0 23），分钟，秒和毫秒数。在这些参数中，只有前两个参数（年和月）是必须的。其它参数如果没有提供，则默认值为每月的1号，其它值默认为0。"
sidebarWeight: 32
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/引用数据类型-对象/Date类型/Date.UTC().md"
---
::: v-pre

# Date.UTC()

> 本节目标：理解“Date.UTC()”的核心思路，并能把它用于实际开发或面试表达。

> 说明：原 OneNote 中有图片引用，但图片未包含在导出目录中；本页保留了可用的文字与代码内容。
这个方法同样是根据传入的参数，返回一个表示日期的毫秒数。
传给这个方法的参数，按顺序依次表示年份，基于0开始的月份，月中的哪一天，小时数（0-23），分钟，秒和毫秒数。在这些参数中，只有前两个参数（年和月）是必须的。其它参数如果没有提供，则默认值为每月的1号，其它值默认为0。

```
**特殊参数值的处理：**
```

```
不在0-11之间的月份参数
```

- ```
    不在1-31之间的日期参数
    ```

    ```
    如果表示天的数值参数为0，则表示上个月的最后一天；
    ```

```
不在0-23之间的时间参数
```

```
不在0-59之间的分秒参数
```

:::
