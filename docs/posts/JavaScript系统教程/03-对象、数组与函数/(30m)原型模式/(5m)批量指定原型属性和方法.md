---
title: "批量指定原型属性和方法"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "对象、数组与函数"
description: "可以用一个包含所有属性和方法的对象字面量来重写整个原型对象，如下： 缺点： constructor 属性不再指向 Person 了。这种写法，本质上完全重写了默认的 prototype 对象，因此 constructor 属性也就变成了新对象的 constructor 属性（指向。"
sidebarWeight: 145
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/面向对象程序设计/(30m)原型模式/(5m)批量指定原型属性和方法.md"
---
::: v-pre

# 批量指定原型属性和方法

> 本节目标：理解“批量指定原型属性和方法”的核心思路，并能把它用于实际开发或面试表达。
可以用一个包含所有属性和方法的对象字面量来重写整个原型对象，如下：

```
function Person() { }
Person.prototype = {
name: 'Nicholas',
age: 29,
job: 'Software Engineer',
sayName: function () {
console.log(this.name);
}
};
```

缺点：`constructor`属性不再指向`Person`了。这种写法，本质上完全重写了默认的`prototype`对象，因此`constructor`属性也就变成了新对象的`constructor`属性（指向`Object`构造函数）。此时，尽管`instanceof`操作符还能返回正确的结果，但通过`constructor`已经无法确定对象的类型了。

**解决方法：**
一、覆盖原型对象的时候，重新定义`constructor`属性的值：

```
function Person() { }
Person.prototype = {
constructor: Person,
name: 'Nicholas',
age: 29,
job: 'Software Engineer',
sayName: function () {
console.log(this.name);
}
};
```

二、为确保`constructor`保持不可枚举性，须使用`ES5`的引擎才有的

```
Objcect.defineProperty()
```

方法

```
;
Object.defineProperty(Person.prototype, 'constructor', {
  enumerable: false,
  value: Person
})
```

:::
