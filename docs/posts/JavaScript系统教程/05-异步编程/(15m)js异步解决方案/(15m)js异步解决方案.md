---
title: "js异步解决方案"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "异步编程"
description: "Js 是单线程的 Javascript 语言的执行环境是 \" 单线程 \" ，也必须是“单线程”； 单线程（ single thread ） 一个代码执行器； 从上而下执行代码； 上面的代码未执行完，下面的代码就要等上面的执行完再执行。 如 var t i \\< // 250 （。"
sidebarWeight: 1
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/11-异步编程/(15m)js异步解决方案/(15m)js异步解决方案.md"
---
::: v-pre

# js异步解决方案

> 本节目标：理解“js异步解决方案”的核心思路，并能把它用于实际开发或面试表达。
`Js`**是单线程的**
`Javascript`语言的执行环境是`"`单线程`"`，也必须是“单线程”；
单线程（`single thread`）

- 一个代码执行器；
- 从上而下执行代码；
- 上面的代码未执行完，下面的代码就要等上面的执行完再执行。

如
`var`

```
i,
```

 `t` `=`

```
Date.now()
for
```

```
(i
```

 `=`

```
0;
```

 `i` `\<`

```
100000000;
```

```
i++)
```

```
{
}
console.log(Date.now()
```

 `-`

```
t)
```

  `//` `250` （`chrome`浏览器）
上面这代码中的`console.log`语句，会一直等到上面的循环执行完才会执行，在它执行之前，它等了差不多

```
250ms
```

。
**多线程方案**
为了利用多核`CPU`的计算能力，`HTML5`提出`Web Worker`标准，允许`JavaScript`脚本创建多个线程，但是子线程完全受主线程控制，且不得操作`DOM`。
这个新标准并没有改变`JavaScript`单线程的本质。

**为什么是单线程**
作为浏览器脚本语言，`JavaScript`的主要用途是：

- 响应用户交互；
- 数据校验；
- 数据交互`ajax;`
- 操作`DOM`。

这决定了它只能是单线程，否则会带来很复杂的同步问题。
比如，假定`JavaScript`同时有两个线程，一个线程在某个`DOM`节点上添加内容，另一个线程删除了这个节点，这时浏览器应该以哪个线程为准？所以，为了避免复杂性，从一诞生，`JavaScript`就是单线程，这已经成了这门语言的核心特征，将来也不会改变。

**单线程的****问题**

- 如果某一段代码执行时间很长，那后面的代码就只能等着；
- 比如前面的`js`未执行完，后面渲染网页页面的代码就不会被执行，浏览器只能看到一片空白；
- 但实际上有很多代码的执行需要很长时间，如：

    - 网络请求，如`ajax`
    - `IO` 操作，如`readFile`，`readdir`
    - 定时函数，如`setTimeout` `setInterval`

为了解决这个问题，`Javascript`语言将任务的执行模式分成两种：同步（`Synchronous`）和异步（`Asynchronous`）。

**同步模式**就是代码严格从上至下执行；
**异步模式**

- 是主执行器碰到需要执行很长时间的代码（也就是异步任务）时，马上把异步任务交给另一个次要执行器去执行，然后主执行器继续执行下面的能立即执行并得到结果的代码（同步任务）；
- 同步任务和异步任务有明显的标志，执行器能快速区分；
- 次要执行器把这些异步任务执行完后，会把执行的结果作为参数放传给一个函数，这个函数叫回调函数；次要执行器把这个回调函数放到一个待执行函数队列（任务队列）中去。
- 主执行器的执行完同步任务后，会立即检查这个待执行函数队列中有没有要执行的函数，如果有的话，把这些函数依次执行；
- 在执行任务队列中函数的过程中，可能又会碰到需要执行很长时间的异步任务，又会开始上面的过程。

```
**任务队列**
任务队列可分为宏任务队列和微任务队列。
```

```
当主线程代码执行完之后；
```

会优先执行微任务队列中的任务，微任务队列的任务都执行完之后，再执行宏任务队列中的任务。

在执行宏任务队列中的任务时，每执行完一个宏任务，都会对微任务队列进行检查，

主执行器首先会判断微任务对列中是否有任务可以执行，如果有就将微任务队首的事件压入栈中执行。

执行完之后，会检查微任务队列是否还有待执行函数，如果有就继续执行微任务队列中的函数；

```
如果没有了，就会去执行宏任务队列中的任务；
```

执行完一个宏任务之后，又会对微任务队列进行检查，如果有，则

微任务包括了promise的回调、node中的process.nextTick 、对Dom变化监听的MutationObserver。
宏任务包括了script脚本的执行、setTimeout ，setInterval ，setImmediate 一类的定时事件，还有如I/O操作、UI 渲染等。

:::
