---
title: "使用形式"
date: 2026-08-11
categories:
  - "JavaScript 系统教程"
tags:
  - "JavaScript"
  - "前端"
  - "教程"
  - "OneNote"
  - "异步编程"
description: "foo() \\} 2 \\}; 3 4 。 对象的方法 let obj { async { } 5 。 Class 的方法 class Storage { { } async { const cache await return storage new … )。"
sidebarWeight: 56
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/b-原生js/11-异步编程/async 函数/使用形式.md"
---
::: v-pre

# 使用形式

> 本节目标：理解“使用形式”的核心思路，并能把它用于实际开发或面试表达。
```
async 函数有多种使用形式。
`1`。 函数声明
`async` `function`

```
foo()
```

 `{`

```
\}
2
```

。 函数表达式
`const` `foo` `=` `async` `function` `()` `{`

```
\};
3
```

。 箭头函数
`const` `foo` `=` `async` `()` `=>` `{` `};`
```

`4`。 对象的方法
`let` `obj` `=` `{` `async`

```
foo()
```

 `{` `}`

```
};
obj.foo().then(...)
```

`5`。 `Class` 的方法
`class` `Storage` `{`

```
constructor()
```

 `{`

```
this.cachePromise
```

 `=`

```
caches.open('avatars');
```
     `}`
    `async`

```
getAvatar(name)
```

 `{`
        `const` `cache` `=` `await`

```
this.cachePromise;
```
         `return`

```
cache.match(`/avatars/${name}.jpg`);
```

```
}
}
const
```

 `storage` `=` `new`

```
Storage();
storage.getAvatar('jake').then(
```

…`);`

:::
