---
title: "数据观测"
date: 2026-08-11
categories:
  - "Vue 系统教程"
tags:
  - "Vue"
  - "Vue3"
  - "前端"
  - "教程"
  - "OneNote"
  - "原理与手写实现"
description: "创建一个Observer实例，调用new的时候就会执行constructor，也就是相当于执行了一个函数；在这个contructor中，可以调用类原型上的各种方法； 如果属性很多，就会有很多闭包； 性能优化原则 ： 不要把所有的数据都放在data中，因为所有的数据都会增加get和。"
sidebarWeight: 79
lastUpdated: false
feed: false
source: onenote
sourceNote: "OneNote/f-vue/手写/珠峰/数据观测.md"
---
::: v-pre

# 数据观测

> 本节目标：理解“数据观测”的核心思路，并能把它用于实际开发或面试表达。

> 说明：原 OneNote 中有图片引用，但图片未包含在导出目录中；本页保留了可用的文字与代码内容。
创建一个Observer实例，调用new的时候就会执行constructor，也就是相当于执行了一个函数；在这个contructor中，可以调用类原型上的各种方法；

如果属性很多，就会有很多闭包；

性能优化原则 ：

- 不要把所有的数据都放在data中，因为所有的数据都会增加get和set.
- 不要写数据的时候，层次过深，尽量扁平化数据，会增加递归；
- 不要频繁获取数据，如：

尽量计算好数据后，一次性进行赋值或取值；

- 如果数据不需要响应式，可以使用Object.freeze，冻结属性。

:::
